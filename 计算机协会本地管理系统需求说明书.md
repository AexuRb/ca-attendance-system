# 计算机协会本地管理系统需求说明书

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 2.1 |
| 文档状态 | 当前版本需求基线 |
| 更新日期 | 2026-07-15 |
| 适用范围 | Windows 桌面端、本机 Web 端、受限远程管理端 |
| 对应系统 | 计算机协会本地管理系统 |

## 1. 文档说明

### 1.1 编写目的

本文档用于统一计算机协会本地管理系统的产品范围、角色权限、业务规则、数据要求和验收标准，作为后续开发、测试、交接和版本验收的依据。

本文档描述的是当前系统应保持的完整能力，不再沿用早期“仅签到签退”“依赖 MySQL”或“部长签到需要审核”等旧版约定。

### 1.2 术语约定

- “系统”指计算机协会本地管理系统。
- “主机”指安装并运行桌面应用、后端服务和 SQLite 数据库的 Windows 电脑。
- “本机入口”指主机上的 `http://127.0.0.1:8080`。
- “远程入口”指通过樱花穿透转发 `127.0.0.1:8081` 后形成的受限后台地址。
- “值班记录”指一次签到至签退形成的记录。
- “有效时长”指满足值班日、值班时段、审核和时间完整性要求后计入统计的时长。
- “归档”指业务数据仍保留，但不再出现在默认工作列表中。
- “移入回收站”指维修事务的软删除；“永久删除”指不可在业务页面恢复的实际删除。

### 1.3 需求用语

- “必须”：缺失时视为不满足验收要求。
- “应”：正常情况下应实现，确有技术限制时需记录原因。
- “可”：增强能力，不影响核心流程验收。

## 2. 项目概述

### 2.1 产品定位

系统是面向计算机协会日常工作的 Windows 本地管理后台，以公开签到台为现场入口，以分角色后台承载值班、成员、培训、排班、维修、统计和数据维护工作。

系统以单台主机为数据中心，日常运行不依赖互联网、MySQL、外部 Java 环境或浏览器插件。需要异地管理时，可通过单独的受限端口接入后台，但公开签到台始终只允许主机本地使用。

### 2.2 建设目标

1. 降低现场误签、漏签和重复提交的概率。
2. 将签到审核、时长统计、成员维护和排班管理统一到一个系统中。
3. 将培训时长计入协会统一值班统计。
4. 规范维修事务登记、协议预览、打印、归档和删除流程。
5. 通过角色权限下放减少会长的日常审核和维修登记负担。
6. 通过 SQLite、备份和完整目录迁移降低换届交接成本。
7. 在不开放远程签到台的前提下，为会长和管理员提供受限远程管理能力。

### 2.3 使用规模

- 部署形态：一台 Windows 主机运行一套系统。
- 现场设备：同一时间仅使用主机作为公开签到设备。
- 远程管理：仅会长和管理员可用，建议同时在线人数不超过 10 人。
- 网络条件：核心功能必须可在主机完全离线时运行。

### 2.4 功能范围

系统范围包括：

- 首次初始化、登录、修改密码和账号停用控制；
- 本机公开签到台、同名选择、两步确认和自动复位；
- 签到审核、记录查询、手动补录、修改、删除和有效时长计算；
- 成员账号、角色、状态、资料和 Excel 批量导入；
- 标准统计、自定义 Excel 导出和导入模板；
- 培训场次、参与名单、主讲人和培训时长；
- 值班星期、值班时间段、部长排班和排班 Excel 导入；
- 维修事务、维修协议、免责协议、回收站和协议预览打印；
- 操作日志、完整备份、恢复、数据迁移和桌面运行日志；
- Windows 桌面启动、关闭到托盘和受限远程后台；
- 可配置的鸣谢信息展示。

### 2.5 暂不包含

当前版本不包含：

- 协会资料库、通用事务清单或材料审批模块；
- 培训负责人角色；
- 成员或部长的远程后台访问；
- 局域网或公网签到；
- 手机原生应用和非 Windows 桌面发行包；
- MySQL 数据库迁移和 SQLCipher 数据库加密；
- 操作日志一键回滚；
- 云同步、在线消息、邮件或短信通知；
- 自动识别毕业年份并停用账号。

## 3. 系统架构与运行方式

### 3.1 技术组成

- 桌面容器：Electron。
- 前端：Vue 3、Vite、Vue Router。
- 后端：Spring Boot 单体服务。
- 数据访问：Spring JDBC。
- 数据库：嵌入式 SQLite。
- Excel：Apache POI 生成和解析 `.xlsx`。
- 运行环境：桌面发行包内置 Java 21 运行时。

### 3.2 运行端口

| 端口 | 监听地址 | 用途 | 可见内容 |
| --- | --- | --- | --- |
| 8080 | `127.0.0.1` | 主机本地入口 | 公开签到台和完整后台 |
| 8081 | `127.0.0.1` | 受限远程连接器 | 后台登录及会长、管理员权限范围内的功能 |

两个端口都必须只监听回环地址，不得直接开放到局域网。

### 3.3 本机运行

- 桌面应用启动时必须自动启动后端、等待服务可用并打开签到台。
- 同一主机可同时通过桌面窗口或普通浏览器访问本机入口。
- 系统必须限制为单实例运行，重复启动时应恢复已有窗口或给出明确提示。
- 主机无互联网连接时，签到、后台管理、导入导出、协议预览和备份必须正常工作。

### 3.4 远程运行

- 樱花穿透只能转发 `127.0.0.1:8081`，不得转发 `8080`。
- 远程入口必须直接显示后台登录，不得展示签到台。
- 远程入口只允许会长和管理员登录。
- 成员或部长即使持有从本机取得的有效令牌，也不得通过远程端口调用后台接口。
- 首次初始化、公开签到、签到查询、桌面控制接口不得通过远程端口使用。
- 远程读取排班数据时必须先由会长或管理员登录。

## 4. 用户角色与权限

### 4.1 角色定义

| 角色 | 角色定位 |
| --- | --- |
| 成员 `MEMBER` | 使用公开签到台，登录后维护个人资料并查看本人记录 |
| 部长 `MINISTER` | 承担日常审核、当周记录纠错、标准统计和维修事务管理 |
| 会长 `PRESIDENT` | 负责协会业务管理、成员管理、培训、排班、数据导出和备份 |
| 管理员 `ADMIN` | 负责系统最高权限、管理员账号、日志、备份恢复和永久删除 |

### 4.2 权限原则

1. 后端必须执行真实权限校验，前端隐藏菜单不能替代接口鉴权。
2. 会长可以任命成员、部长和会长，但不能任命或维护管理员。
3. 只有管理员可以任命管理员或修改管理员账号。
4. 成员毕业或离开协会后应停用账号，不应删除其历史记录。
5. 部长不得进入成员、培训、排班、数据中心和值班设置页面。
6. 部长不得修改值班星期、值班时间段或排班。
7. 远程入口在原有角色权限之外，再增加“仅会长和管理员”限制。

### 4.3 权限矩阵

| 功能 | 成员 | 部长 | 会长 | 管理员 |
| --- | --- | --- | --- | --- |
| 本机公开签到/签退 | 是 | 是 | 是 | 是 |
| 远程后台登录 | 否 | 否 | 是 | 是 |
| 查看和修改个人资料 | 是 | 是 | 是 | 是 |
| 查看本人值班记录 | 是 | 是 | 是 | 是 |
| 查看“今日”后台概览 | 否 | 是 | 是 | 是 |
| 审核普通成员签到 | 否 | 是 | 是 | 是 |
| 查询全部值班记录 | 否 | 是 | 是 | 是 |
| 修改/删除值班记录 | 否 | 本周成员和部长记录 | 不限周次和角色 | 不限周次和角色 |
| 手动新增值班记录 | 否 | 否 | 是 | 是 |
| 查看标准统计 | 否 | 是 | 是 | 是 |
| 导出标准统计 | 否 | 是 | 是 | 是 |
| 成员管理与成员导入 | 否 | 否 | 是 | 是 |
| 任命管理员/维护管理员账号 | 否 | 否 | 否 | 是 |
| 删除无值班记录账号 | 否 | 否 | 否 | 是 |
| 培训管理与导入导出 | 否 | 否 | 是 | 是 |
| 排班和时段设置 | 否 | 否 | 是 | 是 |
| 维修事务登记与修改 | 否 | 是 | 是 | 是 |
| 导出维修事务 | 否 | 否 | 是 | 是 |
| 将维修事务移入回收站 | 否 | 否 | 是 | 是 |
| 查看、恢复、永久删除维修回收站 | 否 | 否 | 否 | 是 |
| 自定义 Excel 导出 | 否 | 否 | 是 | 是 |
| 创建、查看、下载备份 | 否 | 否 | 是 | 是 |
| 删除或恢复备份 | 否 | 否 | 否 | 是 |
| 查看、导出或清空操作日志 | 否 | 否 | 否 | 是 |

## 5. 功能需求

### 5.1 首次初始化与认证

#### AUTH-01 首次初始化

- 数据库中没有账号时，本机入口必须显示初始化页面。
- 初始化必须由现场人员创建首位管理员，不得预置公开账号或默认密码。
- 初始化接口只能从主机本地调用，远程入口必须拒绝初始化。

#### AUTH-02 登录

- 用户使用学号或系统账号和密码登录后台。
- 停用账号不得登录，也不得继续使用已签发令牌。
- 登录成功后，系统应按角色展示可用模块。
- 登录令牌默认有效期为 12 小时；退出登录后令牌必须失效。

#### AUTH-03 密码

- 新建或导入的普通成员初始密码默认为学号后 6 位。
- 使用初始密码的账号首次登录后必须先修改密码，完成前不得访问其他后台功能。
- 所有用户都可在个人页面修改自己的密码。
- 会长和管理员可重置非管理员账号密码；管理员账号只能由管理员重置。
- 重置密码后目标账号的现有登录令牌必须失效，并在下次登录时强制修改密码。

#### AUTH-04 记住密码

- 桌面应用中的“记住密码”必须使用 Windows 提供的安全存储能力加密保存账号和密码。
- 普通浏览器中不得以明文形式保存密码，只允许记住账号。
- 加密存储不可用或保存失败时，系统不得降级为明文保存。

#### AUTH-05 远程登录保护

- 远程登录失败必须写入操作日志，成功登录也必须留痕。
- 同一来源与账号组合连续失败 5 次后，第 6 次起应限制登录 10 分钟。
- 登录成功后应清除对应的失败计数。

### 5.2 本机公开签到台

#### KIO-01 入口限制

- 签到台无需后台登录，但只能通过主机本地入口使用。
- 远程入口不得显示、查询或提交公开签到。
- 只有启用状态的账号可以签到或签退。

#### KIO-02 查询与确认

- 输入框支持按学号或姓名查询。
- 查询结果不得直接提交，必须经过“查询成员”和“确认签到/签退”两步。
- 同名成员应以选择列表展示，只公开姓名和学号后 4 位，不公开完整学号、学院等额外资料。
- 查询不到成员时，应提示检查输入，并建议联系管理员确认账号是否停用。

#### KIO-03 签到和签退判断

- 当天不存在未签退记录时，提交动作应为签到。
- 当天存在一条未签退记录时，提交动作应为签退并完成该记录。
- 提交前必须再次显示姓名和本次动作供用户确认。

#### KIO-04 自动复位

- 页面打开、自动复位和点击“下一位”后必须自动聚焦输入框。
- 提交成功后可短暂显示姓名和结果，但必须在 3 至 5 秒内自动清除；当前实现按 4 秒复位。
- 公用电脑不得长期保留上一位成员的身份信息。

#### KIO-05 连接异常

- 本机服务暂时不可用时，应保留用户已经输入或确认的信息。
- 连接恢复后应自动重试，也应提供“立即重试”按钮。
- 提交重试必须复用同一提交编号，避免网络抖动产生重复签到或签退。

#### KIO-06 排班展示

- 签到台应显示后台设置中的今日值班时间段及对应部长名单。
- 不得在前端写死 `14:00-16:00` 等固定时段。
- 今日排班必须按已设置的时间段分组，显示时段、人数和姓名。
- 姓名下方不重复显示个人时间；时间只显示在时段标题中。
- 页面底部应显示本周各值班日的排班概览。

### 5.3 值班记录与时长

#### ATT-01 审核状态

- 普通成员签到和签退后的状态为“待审核”。
- 部长、会长和管理员签到和签退后自动通过，无需人工审核。
- 驳回记录时必须填写原因。
- 部长不能审核自己的记录。

#### ATT-02 非值班时间测试

- 非值班日或当前不在值班时段时，系统仍允许提交签到和签退，便于现场测试。
- 此类公开提交必须明确提示“默认不计入有效时长”。
- 是否处于值班时段，以签到发生时后台已保存的时间段为准，并随记录保存资格快照。

#### ATT-03 有效记录条件

公开签到记录必须同时满足以下条件才可计入有效时长：

1. 签到日期是已启用的值班日；
2. 签到时间处于已设置的值班时间段；
3. 已完成签退，且签退时间晚于签到时间；
4. 签到和签退均已通过或自动通过；
5. 签到或签退均未被驳回。

不满足条件的记录应标记为待审核、未完成或无效，且有效时长为 0。

#### ATT-04 时长计算

- 原始时长按签到至签退之间的实际分钟数计算。
- 值班有效小时按最近整数小时计算，30 分钟及以上向上取整，低于 30 分钟向下取整。
- 示例：29 分钟计 0 小时，30 分钟计 1 小时，89 分钟计 1 小时，90 分钟计 2 小时。
- 修改签到、签退或审核状态后，系统必须重新计算有效时长。

#### ATT-05 手动新增

- 只有会长和管理员可以手动新增值班记录。
- 新增时必须选择启用账号、填写签到时间和操作原因。
- 所选日期必须是当前设置的值班日。
- 签退时间可暂不填写；填写时必须晚于签到时间。
- 手动新增记录默认自动通过，并写入操作日志。

#### ATT-06 手动修改

- 部长、会长和管理员可以修改值班记录，修改时必须填写原因。
- 部长只能修改本周内普通成员或部长的记录，且修改后的日期也必须仍在本周。
- 部长不能修改会长或管理员的记录。
- 部长修改后的签到和签退状态保持自动通过。
- 会长和管理员不受周次和目标角色限制。

#### ATT-07 删除

- 删除值班记录必须填写原因。
- 部长只能删除本周普通成员或部长的记录。
- 会长和管理员可以删除任意日期和角色的记录。
- 删除前系统必须自动创建安全备份，并在日志中记录备份文件名、操作人和原因。

### 5.4 今日概览、审核与记录查询

#### DUTY-01 今日概览

- “今日”页面只对部长、会长和管理员开放。
- 页面应优先展示真实待处理事项，包括待审核、未签退、今日值班记录和处理中维修。
- 本年排行不属于今日页面；首页应以今日值班记录替代长期排行。
- 部长无排班权限，因此其今日页面不得显示“排班待补充”事项。
- 会长和管理员可看到缺失排班提醒。
- 页面数据应每 30 秒自动刷新，并在窗口重新获得焦点或页面重新可见时刷新，不依赖手动同步按钮。

#### DUTY-02 审核

- 部长、会长和管理员可查看待审核签到和签退。
- 审核支持单条通过、单条驳回和批量通过。
- 部长的审核列表不得包含其本人记录。
- 审核后应立即重新计算记录状态和有效时长。

#### DUTY-03 记录查询

- 部长、会长和管理员可按日期、学号、姓名和有效状态查询记录。
- 系统应提供未签退记录查询，便于当天闭班核对。
- 个人页面中的记录区与后台记录列表应使用一致的信息结构和状态表达。

### 5.5 成员与个人资料

#### MEM-01 成员资料

成员资料至少包括：学号、姓名、角色、账号状态、手机号、学院或专业、年级和 QQ。维修事务不使用成员的学院字段作为送修单位。

#### MEM-02 成员管理

- 成员页面只对会长和管理员开放。
- 支持新增、搜索、按角色/状态/年级筛选、编辑资料、调整角色、重置密码和批量启停账号。
- 会长不得修改管理员账号，也不得把任何账号任命为管理员。
- 只有管理员可维护管理员账号。
- 账号被停用时，系统必须撤销其登录令牌，但保留历史值班、培训和业务数据。
- 批量停用前应自动创建安全备份。

#### MEM-03 删除账号

- 只有管理员可以删除账号。
- 当前登录账号不得删除。
- 已有值班记录的账号不得删除，应改为停用。
- 删除前必须自动创建安全备份并记录操作日志。

#### MEM-04 Excel 导入

- 会长和管理员可下载成员导入模板并上传 `.xlsx` 文件。
- 姓名和学号为必填字段，学号在文件内不得重复。
- 新成员默认角色为成员、状态为启用、初始密码为学号后 6 位，并要求首次登录修改密码。
- 已存在成员只更新基础资料，不自动修改角色、账号状态和密码。
- 导入结果必须报告新增、更新、跳过数量及可理解的错误原因。

#### PRO-01 个人页面

- 所有角色均可进入个人页面。
- 用户可查看自己的账号信息、值班记录、值班时长和培训时长。
- 用户可修改手机号、学院或专业、年级、QQ 和个人密码。
- 个人页面的布局和信息层级应与其他后台页面保持一致，不使用特殊的超长页面结构。

### 5.6 统计与导出

#### STA-01 标准统计

- 部长、会长和管理员可按日期范围查看标准统计。
- 汇总至少包含姓名、学号、年级、有效总次数和有效总时长。
- 有效总时长为有效值班时长与未归档培训时长之和；有效总次数为有效值班次数与对应培训参与次数之和。
- 选择“本周”时，应按日期显示每名成员的时长矩阵，便于核对汇总结果。

#### STA-02 标准统计导出

- 部长、会长和管理员均可导出标准统计 Excel。
- 导出内容必须使用当前筛选日期范围。
- 标准导出应包含学号、姓名、日期时长列和有效总时长，其数值必须与相同日期范围的页面统计一致。

### 5.7 培训管理

#### TRN-01 权限

- 培训页面只对会长和管理员开放。
- 不设置培训负责人角色，部长不得查看或管理培训。

#### TRN-02 培训场次

- 支持新增、编辑、查看和归档培训场次。
- 培训场次至少包含标题、日期、开始时间、结束时间、地点、主讲人和说明。
- 归档后的场次不出现在默认列表和标准时长统计中，但数据仍保留，不执行物理删除。

#### TRN-03 参与名单

- 每名参与者至少记录学号、姓名、计入时长和备注。
- 前端不显示“状态”和“来源”字段，也不要求用户维护这两个字段。
- 同一培训场次内学号不得重复。
- 培训时长允许按 0.25 小时等小数步长录入，并计入该成员的总时长。
- 未填写个人时长时，可使用培训开始和结束时间计算默认时长。

#### TRN-04 Excel 导入导出

- 系统必须提供培训名单导入模板。
- 模板第一条数据默认为或优先预填主讲人。
- 导入识别学号、姓名、时长和备注；同场已存在学号时更新对应记录。
- 支持导出单场参与名单和指定日期范围内的培训统计。

### 5.8 排班与值班设置

#### SCH-01 值班星期

- 会长和管理员可设置每周哪些星期为值班日。
- 部长只能查看与自身工作相关的结果，不能修改值班星期。
- 签到台、后台今日页面和统计规则必须使用同一份星期设置。

#### SCH-02 值班时间段

- 会长和管理员可新增、编辑、排序、启用和停用值班时间段。
- 每个时段必须包含开始时间和结束时间，结束时间必须晚于开始时间。
- 设置保存后，签到台、后台排班和有效时长判断必须读取同一份数据。
- 部长不得修改值班时间段。

#### SCH-03 排班

- 排班页面只对会长和管理员开放。
- 新增或编辑排班时，只允许选择已保存且启用的时间段，不允许自由输入其他时间。
- 每个排班分组由星期、时间段和一名或多名人员组成。
- 排班人员只允许使用启用状态的部长、会长或管理员账号；必要时可保留纯姓名展示兼容历史数据。
- 排班可控制是否在公开签到台显示，也可归档。
- 签到台和后台必须按时间段对新增排班正确分类，不能遗漏非默认时段。

#### SCH-04 排班 Excel 导入

- 系统必须根据当前值班星期和时间段生成导入模板。
- 导入文件每行填写一人，学号必填，姓名可选。
- 只允许导入启用状态的部长、会长或管理员。
- 相同星期和时间段的多行必须合并为一个多人排班分组。
- 系统只覆盖文件中实际出现的分组，其他已有排班保持不变。
- 导入前必须完成全量校验和预览；任意一行存在错误时，整份文件不得写入。

### 5.9 维修事务与协议

#### REP-01 事务字段

- 部长、会长和管理员可新增、查看和修改维修事务。
- 维修事务至少包含：维修编号、协议类型、送修人、联系方式、设备类型、品牌、型号、随附物品、故障描述、处理记录、数据备份确认、风险确认、隐私确认、处理人、接收时间、完成时间和备注。
- 不提供“单位/学院”和“设备序列号”输入字段。
- 状态只保留“进行中”“已完成”“已取消”三种。

#### REP-02 编号

- 新建维修事务时必须自动生成全局可识别的维修编号。
- 编号格式为 `JXWXyyyyMMdd-0001`，同一天按四位序号递增。
- 事务列表、导出文件和协议预览必须使用同一编号。

#### REP-03 协议

- 协议类型只分为“维修协议”和“免责协议”。
- 协议正文必须以协会提供的《个人电脑维修协议书（2024版）》和《设备维修协议书（2024版）》为依据。
- 协议预览必须自动填入维修编号、送修人、联系方式、设备信息、故障描述、处理人和日期等已有数据。
- 桌面应用内必须可预览协议，并支持调用打印功能。
- 协议页面应适配 A4 打印，信息区、正文和签字区应清晰分隔。

#### REP-04 导出与删除

- 部长不能导出或删除维修事务。
- 会长和管理员可按日期范围导出维修事务 Excel。
- 会长和管理员可将维修事务移入回收站。
- 只有管理员可以查看回收站、恢复事务或永久删除。
- 永久删除前必须输入完整维修编号确认，系统必须先自动创建安全备份。

### 5.10 数据中心与自定义导出

#### DAT-01 数据中心

- 数据中心只对会长和管理员开放。
- 数据中心应集中提供模板下载、自定义导出、备份列表、备份创建、下载和恢复入口。
- 不应在“数据”和“维护”两个页面重复展示同一组备份功能。

#### DAT-02 自定义 Excel 导出

- 导出流程应按“选择数据源、设置筛选、选择字段、预览、导出”组织。
- 会长和管理员可选择成员、值班记录、培训记录、部长排班和维修事务作为数据源。
- 操作日志数据源只对管理员开放。
- 用户必须至少选择一个字段，可按数据源设置日期、角色、状态、星期或关键词等筛选。
- 导出前应显示字段和前若干行预览，并显示预计总行数。
- 单次导出结果不得超过 50,000 行；超过时应提示缩小筛选范围。
- 用户可填写文件名，系统必须清理 Windows 文件名中的非法字符并生成 `.xlsx` 文件。

### 5.11 操作日志

#### LOG-01 记录范围

- 关键后台操作必须记录操作人、操作类型、对象、修改前数据、修改后数据、原因和时间。
- 应记录的操作至少包括：成员新增/修改/停用/删除、角色调整、密码重置、签到审核、记录补录/修改/删除、培训、排班、维修、导入导出、备份恢复和远程登录事件。

#### LOG-02 查询与维护

- 只有管理员可以查看、筛选、导出和清空操作日志。
- 日志支持按关键词、操作类型和日期范围筛选，并分页展示。
- 清空日志前必须自动创建安全备份。
- 当前版本不提供根据日志自动回滚业务数据的功能。

### 5.12 备份、恢复与迁移

#### BAK-01 创建与查看

- 会长和管理员可创建、查看和下载完整备份。
- 备份必须包含成员、签到、培训、排班、维修、操作日志和值班设置等核心数据。
- 备份文件保存到应用根目录的 `backups/app/`。

#### BAK-02 恢复与删除

- 只有管理员可以删除或恢复备份。
- 恢复只接受系统生成且结构有效的 `backup_*.zip` 文件。
- 恢复前必须自动备份当前数据。
- 恢复过程必须在事务中完成；任一步骤失败时不得留下部分恢复结果。
- 恢复成功后应撤销全部登录令牌，要求所有账号重新登录。

#### BAK-03 完整迁移

- 应用根目录应包含 `app/`、`data/`、`backups/`、`exports/` 和 `logs/`。
- 迁移电脑前必须从托盘菜单“完全退出”，确认后端已经停止，再复制完整根目录。
- 应用运行期间不得直接复制 `attendance.db` 作为可靠备份。
- 更新程序时不得覆盖 `data/` 和 `backups/`。

### 5.13 桌面应用

#### DES-01 启动

- 桌面应用必须自带后端程序和 Java 运行时。
- 启动时应创建数据、备份、导出和日志目录，并检查端口占用。
- 后端启动失败时，应显示可理解的错误信息和日志路径。

#### DES-02 关闭到托盘

- 点击窗口右上角关闭按钮时，应用必须隐藏到 Windows 托盘，后端继续运行。
- 首次隐藏到托盘时，应提示“系统仍在运行”。
- 点击托盘图标或“打开管理系统”应恢复窗口。
- 只有托盘菜单“完全退出”才应停止后端并退出应用。

#### DES-03 协议预览与外部导航

- 桌面应用应在受控窗口中显示维修协议预览并支持打印。
- 应阻止页面任意导航到外部网站或打开不受控窗口。
- 桌面渲染进程不得直接获得 Node.js 权限。

### 5.14 鸣谢

#### UI-01 鸣谢入口

- 登录页和后台应提供不干扰主要任务的小型“鸣谢”按钮。
- 点击后使用应用内对话框展示贡献者，不跳转外部页面。
- 贡献者内容应从独立配置文件读取，便于后续增删。
- 当前配置至少包含“2025 会长 陈禹杭”及其项目贡献说明。

## 6. 核心数据要求

### 6.1 主要实体

| 实体 | 关键内容 |
| --- | --- |
| 用户 | 学号、姓名、密码摘要、角色、状态、联系方式、年级、首次改密标记 |
| 值班记录 | 用户快照、日期、签到/签退、审核状态、值班资格快照、分钟数、有效小时 |
| 培训场次 | 标题、日期、时间、地点、主讲人、说明、归档状态 |
| 培训参与者 | 用户快照、时长、备注 |
| 值班星期 | 星期、是否启用 |
| 值班时间段 | 名称、开始时间、结束时间、排序、是否启用 |
| 排班 | 星期、时间段、人员列表、公开显示、归档状态 |
| 维修事务 | 维修编号、协议、送修信息、设备信息、确认项、状态、处理人、删除信息 |
| 操作日志 | 操作人、动作、对象、前后数据、原因、时间 |
| 系统配置 | 初始化状态、结构版本及其他本地配置 |

### 6.2 数据快照

- 值班、培训、排班和维修记录应保存必要的姓名或学号快照，避免成员改名或停用后历史记录失去可读性。
- 值班记录应保存签到发生时是否为值班日、是否处于值班时段的快照。
- 删除成员账号不应破坏历史业务数据的可追溯性。

### 6.3 SQLite

- 主数据库固定保存为应用根目录 `data/attendance.db`。
- 首次启动自动创建数据库并执行版本化结构迁移，不要求用户手动执行 SQL。
- 数据库必须启用外键约束并在启动时进行基本完整性检查。
- 数据库、备份和导出不得提交到公开 GitHub 仓库。

## 7. 非功能需求

### 7.1 离线与依赖

- 核心页面不得依赖 CDN、在线字体、远程图片或第三方在线 API。
- 安装后的日常使用不得要求单独安装 MySQL、Java、Node.js 或开发工具。
- 协议、图标、模板和品牌资源必须随应用本地提供。

### 7.2 安全与隐私

- 密码必须保存为不可逆摘要，不得写入数据库、日志或导出文件的明文字段。
- 成员页面和成员自定义导出包含隐私信息，只允许会长和管理员访问。
- 签到台同名选择只显示学号后 4 位。
- 远程入口建议叠加樱花隧道提供的 HTTPS 或额外认证，不需要远程管理时应关闭隧道。
- SQLite 数据库和系统备份当前未加密，必须通过 Windows 账号、文件夹权限和物理管理限制访问。
- 桌面安装包当前没有代码签名证书，安装时可能出现 Windows 安全提示；发行说明中必须明确该限制。

### 7.3 数据完整性

- 公开签到提交必须具备幂等性，同一提交编号不得生成重复业务记录。
- 批量排班导入和备份恢复必须保证全成或全败。
- 删除值班记录、永久删除维修事务、批量停用、删除账号、清空日志和恢复备份前必须创建安全备份。
- 所有会改变关键业务数据的接口必须校验输入、角色和目标对象状态。

### 7.4 可用性

- 后台一级导航按“值班、人员、事务、系统”分组，并根据角色隐藏无权限模块。
- 后台模块使用独立 URL 路由；刷新、浏览器前进/后退和直达链接应保持当前模块。
- 表单必须使用可点击的真实标签，日期范围明确标注开始和结束日期。
- 表单校验失败时应显示字段级错误并聚焦第一个错误字段。
- 存在未保存表单时，切换模块或关闭页面应提示确认。
- 删除、重置密码、清空日志、恢复等危险操作必须使用应用内确认对话框，不使用浏览器原生 `prompt` 或 `confirm`。
- 页面以桌面端操作效率为主，同时保证常见浏览器宽度下不出现内容遮挡和无意义横向滚动。

### 7.5 视觉与动效

- 整体保持淡蓝色品牌基调，但业务表单和表格应使用干净背景，避免重复工程网格造成视觉干扰。
- 动效用于页面切换、状态变化、提交反馈和对话框，不得延迟高频操作。
- 动态结果必须提供明确文本状态，不能只依赖颜色或动画表达成功、失败和离线。
- 图标按钮必须提供标题或无障碍名称。

### 7.6 日志与故障排查

- 桌面运行日志保存到 `logs/desktop.log`。
- 后端运行日志保存到 `logs/backend.log`。
- 启动失败、端口占用、数据库完整性失败和后端异常退出时，应指向对应日志路径。
- 运行日志不得记录明文密码、完整令牌或协议中的不必要隐私内容。

## 8. 验收标准

### 8.1 初始化与离线运行

1. 在一台未安装 MySQL 和 Java、且断开互联网的 Windows 电脑上启动发行包。
2. 系统自动创建数据目录和 SQLite 数据库，并显示首次管理员创建页面。
3. 创建管理员后可进入本机签到台和后台，各核心模块可正常打开。

### 8.2 签到台

1. 输入普通成员学号，完成两步确认后生成待审核签到记录。
2. 再次查询同一成员时，动作显示为签退。
3. 成功页在 3 至 5 秒内自动清除并重新聚焦输入框。
4. 同名查询只显示学号后 4 位。
5. 中断后端连接后，输入或待提交信息不丢失；恢复连接后可自动或手动安全重试，且不产生重复记录。

### 8.3 值班规则与部长权限

1. 部长签到和签退自动通过，不出现在待审核列表。
2. 普通成员记录可由部长审核，部长不能审核自己。
3. 部长可修改或删除本周普通成员及部长记录，不能处理会长、管理员或非本周记录。
4. 部长不能进入成员、培训、排班、数据中心或设置页面，也不能调用对应接口。
5. 部长可查看并导出标准统计，可管理维修事务，但不能导出或删除维修事务。

### 8.4 排班与设置同步

1. 会长或管理员新增一个非默认时间段并保存。
2. 排班页面只能选择该已保存时段，不能手输其他时段。
3. 为该时段新增多人排班后，后台和签到台均按该时段正确分组显示。
4. 姓名下方不重复显示个人时间。
5. 排班 Excel 出现任意错误时无法确认导入，数据库不发生部分写入。

### 8.5 培训

1. 会长或管理员创建培训场次并下载名单模板。
2. 模板第一条数据预填或默认为主讲人。
3. 导入学号、姓名、时长和备注后，页面不要求维护状态或来源。
4. 培训时长正确计入个人统计和总时长。
5. 部长访问培训页面或接口时被拒绝。

### 8.6 维修与协议

1. 部长创建维修事务，系统自动生成符合规则的维修编号。
2. 状态只能选择进行中、已完成或已取消，表单不出现单位/学院和序列号。
3. 维修协议与免责协议均可在桌面应用内预览并打印，编号与事务编号一致。
4. 会长或管理员可移入回收站，只有管理员可恢复或永久删除。
5. 永久删除必须输入完整编号，并在删除前生成备份。

### 8.7 数据、日志与备份

1. 会长可执行自定义 Excel 预览和导出，但不能选择操作日志数据源。
2. 管理员可选择操作日志并导出。
3. 部长只能导出标准统计，不能进入数据中心。
4. 会长和管理员可创建、查看和下载备份；只有管理员可删除和恢复。
5. 恢复前自动生成当前数据备份，恢复完成后所有账号需要重新登录。

### 8.8 远程入口

1. 樱花穿透仅转发 `8081` 后，远程地址直接显示后台登录。
2. 成员和部长登录被拒绝，会长和管理员可正常登录并保留其后台权限。
3. 远程地址无法打开或调用公开签到、首次初始化和桌面控制功能。
4. 将本机成员或部长令牌复制到远程请求仍被后端拒绝。
5. 连续失败达到限制后返回明确的稍后重试提示，并留下操作日志。

### 8.9 桌面托盘与迁移

1. 点击窗口关闭按钮后窗口隐藏，托盘图标存在，`8080` 服务继续可访问。
2. 点击托盘图标可恢复窗口。
3. 选择“完全退出”后后端停止，`8080` 和 `8081` 均不可访问。
4. 完全退出后复制整个应用根目录到另一台电脑，启动后成员、排班和备份可正常读取。

## 9. 发布与交接要求

- 每次发布前应同步更新本说明书、`README.md`、系统使用说明和本地运行说明中的权限与运行方式。
- 发行包不得包含真实数据库、真实备份、成员隐私、内部材料、本机配置或测试截图。
- 数据库结构变更必须通过版本化迁移脚本完成，不得要求用户手工清库。
- 更新发行包时应优先只替换应用程序目录，保留用户的 `data/` 和 `backups/`。
- 换届交接至少应完成：完整备份、管理员账号交接、角色与停用账号复核、远程隧道凭据交接和恢复演练。
- 未经明确要求，不应在日常功能修改后自动打包、提交 GitHub 或发布 Release。

## 10. 需求变更规则

1. 新需求应先确认涉及角色、数据、页面、导入导出和历史兼容范围。
2. 权限变更必须同时检查前端导航、页面操作、后端接口和自动化测试。
3. 时长、审核、删除和备份规则属于高风险业务规则，修改时必须补充对应测试和验收场景。
4. 文档与代码发生冲突时，应先确认最新业务决策，再同步修改实现和本文档，不能长期保留两套口径。
