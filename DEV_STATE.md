# 开发状态

截至 2026-08-24，项目已完成任务 1 至任务 13 的全部整改并形成 3.0.0 发行基线：系统继续作为 Windows 本地离线管理平台运行，技术栈为 Electron 43、Vue 3/Vite 8、Temurin JRE 21、Spring Boot 4/Spring JDBC 与 SQLite；本机入口固定为 `127.0.0.1:8080`，受限远程管理入口为 `127.0.0.1:8081`，远程仅允许会长和管理员且不开放签到台。已完成数据备份恢复与事务审计加固、认证和权限边界收口、后端职责拆分、前端交互与可访问性精修、培训和维修大数据工作区、桌面启动及窗口稳定性、查询导出上限、遗留接口清理和 CI 动态质量门禁；值班时段继续保持用户人工顺序。3.0.0 已统一写入前端、后端、桌面端及 lockfile，`plan.md` 已加入 `.gitignore` 并从版本控制移除但保留本机文件，测试夹具不再使用真实成员账号。发布验证结果为后端 59 个套件 296 项、前端 73 个文件 226 项、脚本 19 项、桌面端 23 项测试通过，类型检查、生产构建、70 个静态文件重复构建一致性、Maven 依赖上界、PowerShell/Python/桌面 JavaScript 语法和两端生产依赖审计通过；隔离 API 冒烟 24 个流程、7 个核心页面双视口、真实后端四角色 UI 权限回归、性能绝对门禁和 Electron 五类真实进程稳定性场景通过。发行脚本已生成 `CA-Attendance-System-Setup-3.0.0.exe`、`CA-Attendance-System-Portable-3.0.0.zip` 与 `SHA256SUMS.txt`，文件名、内部版本、便携目录、禁止数据文件和重新计算的校验值均一致；发行物保留在被忽略的 `release-artifacts/`，不进入 Git。安装包没有代码签名证书，SQLite 主库和备份未加密，仍是 3.0.0 已披露的非阻塞风险；后续开发应从新的独立评审或明确需求开始，不应恢复已完成的 `plan.md` 任务。
