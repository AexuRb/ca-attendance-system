# 开发状态

截至 2026-08-26，当前源码与发行文件版本已统一为 `3.1.0`，本阶段完成后台视觉升级和桌面端旧资源缓存修复：通用页头与按钮已精简统一，值班星期使用微型日历，签到审核使用状态即操作控件，统计默认本周，固定周排班使用单日时间轴与完整名单，维修事务使用高密度分页台账，培训记录使用月份时间带、场次概览和参与名单工作区；权限、业务接口和 SQLite 结构未改变。Electron 现在会在每次启动前清理渲染缓存并为入口添加单次启动标识，避免安装新版后继续加载旧前端接口。前端构建产物已同步到 `backend/src/main/resources/static/`，性能与大数据视觉脚本已同步当前培训和维修 DOM 结构并补充回归测试。发布前已通过后端、前端、桌面和脚本自动化测试、TypeScript 检查、依赖上界与生产依赖审计、生产构建、隔离 API/UI 冒烟、真实角色权限回归、性能基线、大数据四档视口检查及桌面真实进程稳定性验收；安装版、便携版和 `SHA256SUMS.txt` 已生成且校验一致，测试过程只使用仓库外临时 `APP_ROOT`，未操作正式数据库。安装包未签名、SQLite 主库和备份未加密仍是已披露风险；数据库、备份、导出、日志、运行时、发行文件、测试截图、`tasks/`、`plan.md` 和本地配置继续保持忽略，后续前端方向等待用户逐项指定。
