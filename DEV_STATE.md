# 开发状态

截至 2026-08-24，项目已形成 `3.0.0` 同版本替换发行基线：首次启动不再因初始化状态请求短暂失败而跳过首个管理员创建，初始化页与登录页统一淡蓝色认证布局、显式字段标签、密码显隐和焦点反馈；Electron 视觉缩放从会造成零尺寸视口的 `-3～3`、`0～0` 修正为签到台 `1～1`、其他页面 `1～3`，Windows PowerShell 5.1 打包脚本也已按 UTF-8 读取版本并稳定定位中文便携资源。隔离空库已验证初始化、自动进入后台和后续登录，正式 `win-unpacked` 成品已通过调试协议和截图确认 `#/setup` 视口为 `1426×863`、缩放值为 `1`、页面完整可见；本轮后端 294 项、前端 230 项、桌面端 24 项和脚本 19 项测试通过，类型检查、生产构建、Maven 依赖上界、两端生产依赖审计、脚本语法和格式检查通过，隔离完整 API 冒烟 24 个流程、7 个核心页面双视口、四角色真实后端 UI 权限回归、性能绝对门禁及桌面五类真实进程场景通过。新发行物位于被忽略的 `release-artifacts/`：安装版 SHA-256 为 `7b9d8975c75392cf62a1ef99457152b44e379cf782be2b0f5baed4441a435bac`，便携版为 `b83bd992a099574ef4b80e20abca2018d50dfeca0544e797e701487a7d79b4f4`；内部版本、便携目录和禁止数据扫描均通过。本次没有数据库迁移且未触碰正式数据；安装包未签名、SQLite 主库和备份未加密仍是已披露风险，数据库、备份、导出、日志、运行时、安装包、`tasks/`、`plan.md` 和本地配置继续被忽略。下一阶段仍是后台与签到台的前端视觉精修。
