# 计算机协会本地管理系统后续质量提升计划

## 1. 文档状态

- 当前状态：任务 1 至任务 21 已完成；下一步为任务 22“大数据视觉与回归验收”。
- 执行方式：按任务编号依次进行，一次只处理一个任务。
- 需求基线：以《计算机协会本地管理系统需求说明书》和现有系统行为为准。
- 数据原则：测试使用独立测试数据，不把数据库、成员隐私或本机配置提交到仓库。
- 发布原则：除非用户明确要求，否则不主动提交、打包、推送 GitHub 或发布 Release。

## 2. 执行顺序

| 顺序 | 任务 | 主要目标 | 状态 |
| --- | --- | --- | --- |
| 1 | 角色权限验收 | 确认页面、按钮和接口的权限一致 | 已完成 |
| 2 | 签到与时长流程验收 | 确认记录状态、审核和时长计算正确 | 已完成 |
| 3 | 业务模块验收 | 验证排班、培训、维修、导入和导出 | 已完成 |
| 4 | 数据安全演练 | 验证 SQLite 备份、恢复和异常保护 | 已完成 |
| 5 | 后台剩余页面精修 | 完善视觉、窄屏和无障碍体验 | 已完成 |
| 6 | 大数据量与性能测试 | 检查查询、统计和导出的稳定性 | 已完成 |
| 7 | 桌面端稳定性测试 | 验证启动、托盘、退出和迁移 | 已完成 |
| 8 | 文档与发布准备 | 完成交接资料和发布检查表 | 已完成 |
| 9 | 备份可恢复性闭环 | 保证系统生成的备份一定能被同版本恢复 | 已完成 |
| 10 | 成员删除历史保护 | 防止删除成员后培训等历史数据失真 | 已完成 |
| 11 | 批量审核完整性 | 消除“全部通过”静默遗漏 500 条以后记录的问题 | 已完成 |
| 12 | 培训时长口径统一 | 移除隐藏状态语义，以填写时长作为唯一计入依据 | 已完成 |
| 13 | 导出审计补全 | 让所有业务数据导出均可在操作日志中追溯 | 已完成 |
| 14 | 维修事务增量加载 | 保留三栏布局并控制大数据量下的页面规模 | 已完成 |
| 15 | 账号与密码校验统一 | 统一前后端、单条与导入场景的输入边界 | 已完成 |
| 16 | 培训分页接口 | 为培训场次与参与名单建立服务端分页契约 | 已完成 |
| 17 | 培训工作区状态 | 建立可取消请求、URL 状态和独立加载反馈 | 已完成 |
| 18 | 培训页面重排 | 改为场次目录与单场详情的受控高度工作区 | 已完成 |
| 19 | 维修工作区状态 | 将三栏状态改为单状态工作区并复用现有分页能力 | 已完成 |
| 20 | 维修页面重排 | 进行中使用卡片，历史状态使用分页表格 | 已完成 |
| 21 | 两页交互与表单收口 | 修复竞态、重复提交、导入残留和表单反馈 | 已完成 |
| 22 | 大数据视觉与回归验收 | 使用真实规模数据验证桌面端、窄屏和桌面壳 | 待开始 |

---

## 3. 任务 1：角色权限验收

### 目标

确保普通成员、部长、会长和管理员在本机入口与远程入口中的权限符合需求，并且前端展示与后端强制校验保持一致。

### 大致步骤

1. 根据需求说明书整理角色、页面、操作和接口四个维度的权限矩阵。
2. 核对后台导航、路由守卫、页面按钮和操作菜单是否按角色正确显示。
3. 核对后端控制器与服务层，确认不能通过直接调用接口绕过前端限制。
4. 重点验证部长的本周限制、目标角色限制、统计导出权限及无排班权限。
5. 重点验证会长不能任命或维护管理员，管理员拥有日志、恢复和永久删除权限。
6. 验证远程端口只允许会长和管理员登录，且不开放签到台、初始化和桌面控制接口。
7. 为关键允许和拒绝场景补充后端集成测试与前端组件测试。

### 验收标准

- 权限矩阵与需求说明书一致，无相互矛盾的描述。
- 隐藏入口不能通过手工输入地址或直接请求接口绕过。
- 本机和远程入口的权限差异均有自动化测试。
- 三类后台角色的关键允许、拒绝和边界场景全部通过。

### 预计产物

- 权限矩阵文档。
- 权限相关自动化测试。
- 必要的前后端权限修复及验证记录。

### 完成记录

- 已建立页面、操作和后端权限的完整角色矩阵，覆盖普通成员、部长、会长与管理员。
- 已修复会长可通过成员 Excel 导入覆盖管理员资料的越权入口，同时保留管理员维护管理员资料的正常能力。
- 已补充后端权限策略、真实接口授权、前端导航、路由守卫及维修与维护操作按钮测试。
- 已验证本机与远程入口的关键允许和拒绝场景；后端 106 项测试、前端 85 项测试、类型检查及生产构建均通过。

---

## 4. 任务 2：签到与时长流程验收

### 目标

确认签到、签退、审核、修改、删除和有效时长计算在正常及异常场景下均保持一致。

### 大致步骤

1. 整理普通成员、部长、会长和管理员的签到审核状态规则。
2. 验证值班时间内、值班时间外、跨时段和无排班情况下均能按需求提交签到签退。
3. 验证同名选择、重复签到、未签退、先签退后签到等边界行为。
4. 验证单条审核、全部通过、驳回、手动补录、修改和删除流程。
5. 验证部长只能处理本周普通成员和部长记录，且不能审核自己的记录。
6. 核对每条记录的有效状态、单次时长和统计汇总结果。
7. 检查业务修改、时长重算和操作日志是否处于同一事务中。
8. 补充核心流程的服务层、接口层和前端交互测试。

### 验收标准

- 每种签到状态都有明确且可复现的结果。
- 审核或修改失败时不会留下部分更新的数据。
- 统计时长与有效记录逐条相加的结果一致。
- “全部通过”等批量操作不会漏项、重复处理或越权。

### 预计产物

- 签到状态与时长计算测试表。
- 核心流程自动化测试。
- 数据一致性修复及验证记录。

### 完成记录

- 已建立签到、审核、有效状态和时长舍入矩阵，覆盖普通成员待审核、部长自动通过、重复请求及同日多次签到签退。
- 已允许管理员在系统设置中分别控制“强制值班日”和“强制值班时段”；两个开关默认关闭，会长只读，普通成员与部长无权访问。
- 已将资格与限制规则在签到时保存为记录快照，确保后续修改设置不会反向改变历史记录；旧数据库升级后继续沿用审核决定时长的宽松规则。
- 已修复通过审核误写驳回原因、手动编辑保留过期审核信息、签退时间与未提交状态冲突，以及不足一分钟的正时长被判无效等一致性问题。
- 已验证业务修改、时长重算和审计日志的事务回滚；后端 128 项测试、前端 89 项测试、类型检查及生产构建均通过。

---

## 5. 任务 3：业务模块验收

### 目标

系统验证固定周排班、培训、维修事务、批量导入和自定义导出的完整业务流程。

### 大致步骤

1. 验证固定周排班只允许使用系统设置中已保存的值班星期和时间段。
2. 验证排班新增、修改、删除、多人分组及 Excel 批量导入的原子性。
3. 验证培训主讲人位于表格首行、培训时长正确计入值班统计。
4. 验证维修协议与免责协议的编号、预览、状态和协议内容。
5. 验证维修事务的修改、导出、移入回收站、恢复和永久删除权限。
6. 验证成员、培训、排班模板导入时的预览、错误定位和整批回滚。
7. 验证自定义导出的数据源、日期范围、字段顺序、文件名和 Excel 内容。
8. 为高风险导入、删除和批量操作补充自动化测试。

### 验收标准

- 固定周排班与设置、后台和签到台展示完全同步。
- 导入文件存在任一错误时，不写入部分数据。
- 培训时长只按业务规则计入一次，不重复累计。
- 协议预览在桌面端离线环境中可正常打开。
- 导出的字段、顺序、筛选条件和数据内容一致。

### 预计产物

- 各业务模块验收记录。
- 导入导出与批量操作测试。
- 必要的业务逻辑和界面修复。

### 执行结果

- 已建立《业务模块验收矩阵》，覆盖固定周排班、培训、维修事务、三类 Excel 导入和自定义导出。
- 已修复成员与培训导入的部分写入、固定排班与启用星期不同步、历史免责协议类型显示错误，以及导出预览过期问题。
- 已补充排班新增、修改、归档、导入事务回滚，成员/培训导入整批回滚，协议离线预览和导出一致性测试。
- 后端 139 项测试、前端 93 项测试、类型检查及生产构建全部通过。

---

## 6. 任务 4：数据安全演练

### 目标

验证 SQLite 数据库、完整备份、恢复和维护操作在误操作或异常中断下仍可保护数据。

### 大致步骤

1. 建立一次性测试数据库，写入覆盖全部业务表的可识别测试数据。
2. 执行“创建备份、清空测试数据、恢复备份”的完整往返演练。
3. 对恢复前后的表数量、关键字段、设置项和关联关系进行自动比对。
4. 测试缺失条目、重复条目、未知条目、超大条目和损坏 ZIP。
5. 验证恢复前安全备份、恢复后重新登录及审计日志行为。
6. 检查备份临时文件、失败清理、并发维护操作和一致性快照。
7. 模拟数据库被占用、磁盘写入失败和操作中断时的错误处理。
8. 运行 SQLite 完整性检查，并整理人工恢复步骤。

### 验收标准

- 正常备份可以完整恢复全部受支持数据。
- 非法或损坏备份不会覆盖当前数据库。
- 失败的备份不会出现在可恢复备份列表中。
- 维护操作失败后，数据库仍保持可启动和一致状态。
- 恢复流程具备可重复执行的检查清单。

### 预计产物

- 备份恢复演练记录。
- 数据完整性自动核对工具或测试。
- 故障恢复步骤与风险说明。

### 执行结果

- 已使用一次性 SQLite 数据库完成 12 张受支持表的创建备份、篡改或删除测试数据、恢复和逐项核对，未接触正式数据库。
- 已验证缺失、重复、未知、过多、超大、损坏及高版本备份，非法备份不会覆盖当前数据。
- 已将恢复前安全备份与正式恢复合并到同一个物理事务，并验证约束失败、数据库写锁和文件目录故障不会留下部分恢复。
- 已验证成功恢复后的令牌撤销与审计日志，失败恢复保留当前会话和恢复前安全备份。
- 已完成《数据安全演练记录》和人工故障恢复清单；后端 152 项测试全部通过，无失败、错误或跳过。

---

## 7. 任务 5：后台剩余页面精修

### 目标

在不改变现有整体风格的前提下，继续完善培训、统计、设置、维护和个人资料等页面的细节与一致性。

### 大致步骤

1. 分别检查常规数据、空数据、加载、失败和超长内容状态。
2. 检查桌面端常用分辨率及 390px 窄屏下的布局、弹窗和操作区域。
3. 统一标题、筛选栏、表格、按钮、状态标签和空状态的视觉节奏。
4. 减少无意义说明文字，突出待处理事项和主要操作。
5. 补齐表单标签、行内错误、键盘焦点、动态结果播报和图标语义。
6. 检查弹窗滚动、底部操作固定、长表格操作列和内容溢出。
7. 继续按业务模块拆分组件和样式，避免形成超大单文件。
8. 使用真实浏览器截图和控制台检查进行逐页验收。

### 验收标准

- 页面在主要桌面和手机尺寸下无错位、遮挡和横向页面溢出。
- 主要操作在首屏或明确的操作区域中可发现。
- 表单可通过键盘完成，错误提示能定位到具体字段。
- 浏览器控制台无新增错误或警告。
- 新增组件保持合理职责和代码规模。

### 预计产物

- 各页面视觉验收截图。
- 可访问性与响应式修复。
- 必要的组件和样式拆分。

### 执行结果

- 已检查培训、统计、设置、数据与备份、个人资料、维修事务和操作日志的常规数据、长文本、弹窗及窄屏状态。
- 已修复 768px 临界宽度下两级侧栏挤压内容区的问题，并完善培训长标题、参与名单卡片和值班时段编辑器的响应式布局。
- 已为培训、设置、维修和日志中的图标按钮补齐可访问名称与装饰图标语义，设置校验错误使用即时播报；培训编辑弹窗打开后自动聚焦首个字段。
- 新增独立页面测试和 `admin-detail-polish.css` 精修层，未继续扩大已有大型样式文件；相关页面组件均低于 600 行。
- 已在 1440、1024、768 和 390px 四档视口逐页检查，7 个页面均无横向页面溢出、可见内容溢出或控制台警告与错误。
- 前端 34 个测试文件、95 项测试全部通过，类型检查和生产构建通过；验收使用一次性 SQLite 数据库，未接触正式数据库。

---

## 8. 任务 6：大数据量与性能测试

### 目标

确认系统在协会多年累积数据下仍能流畅查询、统计、导入、导出和备份。

### 大致步骤

1. 使用脚本生成可重复的测试数据，覆盖成员、签到、培训、排班和维修事务。
2. 逐步提升数据规模，记录列表加载、筛选、统计、导出和备份耗时。
3. 检查常用查询是否缺少索引、重复查询或加载了不必要的大字段。
4. 检查前端大列表渲染、分页、搜索输入和弹窗打开时的卡顿。
5. 验证大文件 Excel 导出期间的错误提示和重复点击保护。
6. 检查数据库文件增长、备份体积和内存占用。
7. 只针对已确认的瓶颈进行查询、分页或渲染优化。
8. 优化后使用同一数据集复测并记录对比结果。

### 初始测试规模

- 500 名成员。
- 10,000 条签到记录。
- 500 条培训记录。
- 1,000 条维修事务。
- 多学年范围内的排班和操作日志。

### 验收标准

- 常用页面不会出现明显长时间冻结。
- 查询、统计、导出和备份均能完成且结果正确。
- 性能测试可重复，优化前后有数据对比。
- 测试数据库和生成文件不会进入 Git 仓库。

### 预计产物

- 可重复的数据生成方式。
- 性能基线和瓶颈记录。
- 经验证的针对性优化。

### 执行结果

- 已建立隔离的性能基线工具，自动生成 500 名成员、10,000 条签到、500 场培训、5,000 条培训参与记录、1,000 条维修事务和 5,000 条日志；测试结束后自动清理临时应用目录，未接触正式数据库。
- 已覆盖成员、签到、审核、统计、培训、维修、日志、维护、自定义导出、Excel 导出、完整备份及五个后台页面的接口与浏览器测量，并记录 SQLite 查询计划、数据库体积和进程内存。
- 当前规模下常用 API P95 均低于 100 ms，常用页面完整加载 P95 均低于 700 ms；维修页 8,772 个 DOM 节点被记录为后续增长风险，现阶段未为未证实的瓶颈增加索引或分页。
- 已将全年统计 Excel 的逐列自动测宽和服务端公式计算改为稳定列宽与客户端重算，单次导出由 3,057.26 ms 降至 1,692.88 ms，改善 44.6%，并补充格式与公式集成测试。
- 已完成《大数据量与性能测试记录》，提供环境、规模、性能边界、优化前后数据和一键复测命令。

---

## 9. 任务 7：桌面端稳定性测试

### 目标

验证 Electron 桌面应用在离线 Windows 环境中的启动、运行、托盘、退出和迁移行为。

### 大致步骤

1. 验证首次启动、已有数据启动、后端启动失败和端口占用提示。
2. 验证重复启动时只保留一个应用实例并正确聚焦现有窗口。
3. 验证关闭窗口后最小化到托盘、托盘恢复和完全退出。
4. 验证后台异常退出后桌面端的提示、重试和日志记录。
5. 验证完全离线时签到、后台、协议预览、导入导出和备份可用。
6. 验证根目录数据位置、整目录迁移和新电脑启动流程。
7. 验证本机端口与远程管理端口的监听地址和访问边界。
8. 检查无签名安装包的 Windows 安全提示和用户操作说明。

### 验收标准

- 重复启动不会产生多个后端或多个托盘实例。
- 关闭、恢复和完全退出行为符合界面提示。
- 端口冲突和后端失败时不会静默卡住。
- 整个应用目录迁移后，数据和设置可继续使用。
- 远程入口不能访问签到台或突破角色限制。

### 预计产物

- 桌面端稳定性测试记录。
- 启动与异常处理修复。
- 迁移和常见故障说明。

### 执行结果

- 已建立隔离的桌面真实进程烟测，覆盖首次启动、托盘隐藏与恢复、完整退出、单实例、后端异常停止、回环端口绑定和整目录迁移；测试数据全部位于 `%TEMP%`，正式数据库未被操作。
- 已修复 `8081` 被占用时缺少启动前诊断的问题，现在能区分旧系统运行、`8080` 冲突和 `8081` 冲突。
- 已修复源码桌面端回退到系统 `javapath` 时可能只跟踪 Java 启动器、遗漏实际 JVM 的问题；源码模式现在优先使用仓库内固定 Temurin，与发行包一致。
- 桌面端 16 项单元测试和 4 类真实进程场景全部通过；两个服务端口实测只监听 `127.0.0.1`，正常与异常退出后均无残留 Electron、Java 或监听端口。
- 已完成《桌面端稳定性测试记录》并在《本地运行说明》中加入不生成安装包的复测命令。本轮未构建安装包、未提交或上传。

---

## 10. 任务 8：文档与发布准备

### 目标

为日常使用、故障恢复、换届交接和后续发布准备一套一致、可执行的文档。

### 大致步骤

1. 核对 README、需求说明书、系统使用说明和本地运行说明的一致性。
2. 更新管理员、会长、部长的角色操作说明和权限边界。
3. 编写每日使用、定期备份、恢复演练和异常处理清单。
4. 编写换届交接清单，包括账号、数据目录、备份和管理员数量检查。
5. 整理开发环境、测试命令、构建命令和版本号更新位置。
6. 建立发布前检查表，覆盖测试、隐私文件、静态资源、安装包和版本信息。
7. 检查仓库不包含数据库、备份、导出文件、测试截图、凭据和 `tasks/`。
8. 仅在用户明确确认后执行提交、打包、推送或发布 Release。

### 验收标准

- 文档描述与当前程序行为一致，不保留过时的 MySQL 或旧权限说明。
- 新一届负责人可以仅依据文档完成启动、备份、恢复和迁移。
- 发布前检查表能够明确判断是否适合发布。
- 未经明确授权不执行任何发布操作。

### 预计产物

- 更新后的项目文档。
- 换届交接清单。
- 发布前检查表和版本验证记录。

### 执行结果

- 已统一前端、后端和桌面端版本为 `2.5.0`，更新 README、本地运行说明和变更日志中的发布文件名与使用说明。
- 已补齐《换届交接清单》和《发布检查表》，并将角色权限、签到状态、业务验收、数据安全、性能、视觉与桌面稳定性记录接入 README。
- 已检查 Git 范围与发布包，仓库和便携包均不包含数据库、备份、导出数据、登录凭据、本机配置或 `tasks/`；两个成员导入模板内容一致且仅含示例数据。
- 发布验证通过：后端 153 项、前端 95 项、性能工具 6 项、桌面端 16 项单元测试，以及 24 步完整业务冒烟、7 个核心页面双视口验收和 4 类真实桌面进程场景。
- 已生成安装包、便携包和 `SHA256SUMS.txt`；安装包、内置后端与文件名版本均为 `2.5.0`，两个发布包的 SHA256 与校验文件一致。

## 11. 单项任务通用完成条件

每个任务只有同时满足以下条件后，才标记为完成：

1. 计划范围内的问题已经检查完毕。
2. 发现的问题已修复，或明确记录暂不处理的原因。
3. 相关自动化测试通过。
4. 需要界面验证时，已完成桌面和窄屏浏览器检查。
5. 未误改用户已有内容，未提交数据库、凭据或本机文件。
6. 已向用户汇报改动、验证结果和剩余风险，并等待确认后再进入下一任务。

---

## 12. 本轮 Review 修复范围

### 已确认的原则

1. 优先处理可能造成数据无法恢复、历史数据失真或批量操作遗漏的问题。
2. 培训参与记录不再具有可操作的“状态”和“来源”业务字段，填写的培训时长就是计入统计的时长。
3. 维修事务继续使用当前三栏看板，不改成普通表格分页；通过各栏独立增量加载控制页面规模。
4. 所有成功产生业务数据文件的导出都要写入操作日志；模板下载和协议预览不算业务数据导出。
5. 数据迁移和破坏性场景只在隔离测试数据库或正式数据库副本中验证，不清空或直接改动正式数据库。
6. 本轮不进行无关的界面重做，也不改变既有角色权限。
7. 完成修复不等于自动发布；未经用户明确要求，不提交、打包、推送 GitHub 或发布 Release。

### 依赖顺序

```text
备份可恢复性
    └── 数据迁移与全量回归的安全基础

成员删除历史保护
批量审核完整性
培训时长口径统一
    └── 导出审计补全

维修事务增量加载
账号与密码校验统一
    └── 最终全量回归
```

---

## 13. 任务 9：备份可恢复性闭环

### 目标

建立统一的备份生成与恢复契约，保证“能够成功生成并出现在备份列表中的文件”一定能被同版本程序恢复，避免数据量增长后出现备份可创建但不可恢复的情况。

### 修改方案

1. 将压缩包条目数、表行数、单表解压量和总解压量等限制集中到一个共享的备份限制定义中，生成端与恢复端禁止各自维护不同常量。
2. 备份生成改为流式写入临时文件，避免先把整张表格式化成大块内存数据；全部写入和校验成功后再原子重命名为正式 `.zip`。
3. 恢复端按流式或临时文件方式读取，不再把所有 ZIP 条目同时保存在内存中，同时保留条目数、重复条目、未知条目和解压炸弹防护。
4. 生成过程中同步执行与恢复端一致的行数和字节预算校验；超过契约时明确报错且不把残缺文件加入备份列表。
5. 保持对现有合法旧备份的兼容，并为不兼容、损坏或超限文件提供可理解的错误信息。

### 验收标准

- 当前大数据测试库可以完成“创建备份 -> 列表可见 -> 恢复到隔离库 -> 核对各表数量与关键设置”的闭环。
- 临界容量内成功生成的备份均可恢复，超出容量时在生成阶段失败且不遗留正式 ZIP。
- 恶意高压缩率、重复条目、未知条目和超行数备份仍会被拒绝，过程不会造成明显内存峰值或服务崩溃。

### 验证

- 补充备份生成/恢复同契约测试、边界容量测试、残缺文件清理测试和 ZIP 安全测试。
- 使用隔离数据库执行一次真实恢复演练，并比较恢复前后的表行数、设置和抽样记录。
- 运行后端完整测试与桌面端维护页烟测。

### 预计涉及

- `backend/src/main/java/com/ca/attendance/maintenance/BackupArchiveReader.java`
- `backend/src/main/java/com/ca/attendance/maintenance/BackupArchiveWriter.java`
- `backend/src/main/java/com/ca/attendance/maintenance/DatabaseBackupExporter.java`
- `backend/src/main/java/com/ca/attendance/maintenance/BackupSchema.java`
- 对应维护模块测试

### 执行结果

- 已建立共享备份契约：压缩包、ZIP 条目、单条目解压量、总解压量、单表行数和总行数由同一组限制控制，生成端与恢复端不再各自维护不同阈值。
- 备份生成已改为在一致性数据库事务中逐表、逐行写入 ZIP，并在生成阶段同步检查恢复容量；超过限制时中止且不会发布残缺备份。
- 恢复已改为受限解压到系统临时目录，逐表流式校验和恢复，不再把全部 ZIP 条目、整表 JSON 和整库 Map 同时驻留内存；成功或失败后都会清理临时目录。
- HTTP 上传限制已与 128 MB 备份文件契约对齐，同时保留重复条目、未知条目、路径、解压炸弹和数据行数保护。
- 已用约 24 MB 解压数据复现并修复“系统生成的备份无法被同版本恢复”；维护模块测试和后端 155 项完整测试全部通过。
- 已对正式数据库的只读副本完成真实恢复演练：360 名成员、12,010 条签到、163 场培训、4,934 条培训参与、754 条维修和 1,299 条公开提交等数据恢复前后数量一致，SQLite 完整性与外键检查通过；正式数据库未被修改。

---

## 14. 任务 10：成员删除历史保护

### 目标

成员只要参与过任何需要保留的业务或审计活动，就不能被物理删除，只能停用，确保培训时长、维修经办、排班和操作历史不会因外键置空或关联丢失而失真。

### 修改方案

1. 建立统一的成员关联检查，覆盖签到、培训参与、固定周表、维修经办、操作日志以及业务表中的创建或更新关系。
2. 物理删除仅允许用于从未产生任何业务关联的空白账号；存在关联时返回明确冲突提示，并引导管理员改为“停用账号”。
3. 删除判断与实际删除放在同一事务中，避免检查后到删除前的数据变化形成竞态窗口。
4. 前端根据后端返回的信息使用统一应用内提示，不把“存在培训记录”误报成一般删除失败。

### 验收标准

- 仅有培训记录、仅有排班、仅经办维修、仅留下操作日志的成员都不能物理删除。
- 无任何关联的测试账号可以正常删除，并保留可信的删除审计日志。
- 停用有关联成员后，历史统计、培训明细、维修记录和日志仍能显示原有姓名、账号与时长。

### 验证

- 增加各类“单一关联”删除集成测试、空白账号删除测试和并发事务测试。
- 对成员页执行删除/停用浏览器回归，核对提示、列表刷新和历史页面。

### 预计涉及

- `backend/src/main/java/com/ca/attendance/user/UserService.java`
- 成员控制器、异常响应与相关集成测试
- `frontend/src` 中成员删除交互组件

### 完成记录

- 新增统一成员历史关联检查，覆盖签到及审核、公开签到提交、培训参与和业务留痕、固定周表、维修经办、操作日志、成员管理关系及系统设置更新关系。
- 物理删除只允许无任何历史关联的空白账号；有关联时返回 `409 Conflict`，提示具体关联模块并引导管理员停用账号。
- 删除事务在检查前取得 SQLite 写锁，关联检查、自动备份、删除和审计日志保持在同一事务中，避免并发写入穿过检查窗口。
- 成员页明确说明永久删除边界；后端拒绝删除时保留确认弹窗并展示原始冲突原因，只有删除成功后才关闭弹窗和刷新列表。
- 新增 9 项后端集成场景，覆盖培训、排班、维修、审计、公开签到、业务创建关系、空账号删除、停用保留和并发写入；新增 2 项成员页组件交互测试。
- 后端 166 项测试、前端 97 项测试、前端类型检查和生产构建全部通过；当前环境未提供浏览器自动化接口，浏览器实测未冒充完成。

---

## 15. 任务 11：批量审核完整性

### 目标

修复签到审核“全部通过”只处理前 500 条且不告知用户的问题，使操作范围、实际处理数量和最终反馈完全一致。

### 修改方案

1. 删除业务层静默截断逻辑，按当前筛选条件统计并取得全部待处理目标。
2. 使用数据库批处理分段执行，但将一次“全部通过”作为一个原子业务操作；任一关键步骤失败时回滚本次修改。
3. 返回 `匹配数 / 已处理数 / 已跳过数`，前端确认弹窗显示精确目标数量，完成提示使用后端真实结果。
4. 操作日志只写一条汇总记录，记录审核类型、筛选范围和处理数量，避免大量重复日志。
5. 若未来需要设置硬上限，必须在操作前明确阻止并要求缩小筛选范围，不能静默忽略超限记录。

### 验收标准

- 601 条以上待审核记录执行“全部通过”时不会遗漏第 501 条以后的记录。
- 审核中途模拟异常后，记录状态、有效时长与审计日志全部回滚。
- 页面显示的待审核数量、确认数量、处理结果和刷新后的剩余数量一致。

### 验证

- 增加超过 500 条的批量审核集成测试、失败回滚测试和并发状态变化测试。
- 增加审核页组件测试与真实浏览器操作回归。

### 预计涉及

- `backend/src/main/java/com/ca/attendance/attendance/AttendanceService.java`
- 签到审核接口与集成测试
- `frontend/src` 中签到审核页面及 API 类型

### 完成记录

- 待审核接口改为同时返回可见记录、完整记录数、完整待审核项数和截断状态；页面仍只展示最近 500 条，避免大量 DOM 节点拖慢界面。
- “全部通过”改为由后端按当前角色可审核范围重新取得全部目标，不再提交页面中可见记录的 ID，也不再静默截断第 501 条以后的记录。
- 数据库更新按每批 200 条执行，但整次操作保留在同一事务中；任一状态更新、有效时长重算或汇总日志写入失败都会整体回滚。
- 接口返回匹配记录数、通过审核项数和跳过数；确认弹窗、完成提示及审核后刷新均使用后端完整统计。
- 一次批量操作只写一条 `BULK_REVIEW_ATTENDANCE` 汇总日志，记录处理范围与真实结果。
- 新增 601 条记录无遗漏、等待写锁后的并发新增纳入处理、分段更新失败回滚、汇总日志失败回滚及页面刷新计数测试。
- 后端 169 项测试、前端 98 项测试、前端类型检查和生产构建全部通过；当前环境未提供浏览器自动化接口，未将真实浏览器回归标记为已完成。

---

## 16. 任务 12：培训时长口径统一

### 目标

彻底消除隐藏的培训“出勤状态”对数据理解造成的歧义。培训参与表中的 `duration_hours` 是唯一计入值班统计的依据：大于零就按填写值计入，零时长不计入。

### 修改方案

1. 新增数据库迁移，将历史 `PRESENT / ABSENT / LEAVE` 统一规范为兼容值，同时保持每条记录原有时长不变，避免迁移导致累计时长突然变化。
2. 后端新增和修改参与人时不再接收 `attendanceStatus` 业务输入，统一按培训时长保存；导入模板仍只包含学号、姓名、时长和备注等现有字段。
3. 统计、个人记录和培训汇总统一只依据 `duration_hours`，不再读取隐藏状态决定是否计入。
4. 从前端类型、展示文案和业务导出中移除培训状态；`source` 如需保留导入溯源，仅作为数据库内部元数据，不进入用户操作和统计口径。
5. 更新需求说明与数据字典，明确“参与人记录即培训计时记录”的含义。

### 验收标准

- 相同的培训参与数据在培训页、个人资料、统计页和 Excel 导出中的时长一致。
- 历史请假/缺席标记不会继续形成不可见规则，迁移前后总培训时长保持一致。
- 新建、编辑和导入流程中均不出现状态或来源字段，主讲人首行规则保持不变。

### 验证

- 对包含三种旧状态、零时长和小数时长的数据执行迁移与统计回归。
- 补充培训新增、编辑、导入、个人记录、汇总与导出的端到端测试。
- 在正式数据库副本上比较迁移前后成员培训总时长，不直接修改正式库。

### 预计涉及

- SQLite/Flyway 数据迁移脚本
- `backend/src/main/java/com/ca/attendance/training/TrainingService.java`
- `backend/src/main/java/com/ca/attendance/stats/StatsService.java`
- 培训、个人资料和统计相关前端模块与测试

### 完成记录

- 新增 SQLite V10 迁移，将历史 `PRESENT / ABSENT / LEAVE` 统一为内部兼容值并删除无用状态索引；迁移不重建参与表，也不修改任何 `duration_hours`。
- 培训参与人的新增、编辑和导入请求不再接收 `attendanceStatus`；新增及修改只按时长保存，`source` 继续作为数据库内部导入溯源信息。
- 培训场次、参与人和个人培训记录接口已移除状态、来源及出席/缺席/请假计数；个人资料页同步删除“出勤”列。
- 统一正时长口径：大于 0 的参与记录计入参与人数、培训次数和时长；零时长记录保留在明细中，但不进入任何汇总。
- 单场名单、培训汇总、自定义导出、个人记录和标准统计均只使用 `duration_hours`；旧备份恢复时会规范内部状态但保留原时长。
- 正式数据库保持 V9 且未直接修改；只读基线为 4,934 条培训记录、4,660 条正时长记录、563 条旧非出席状态及 8,928.000000 小时。
- 在正式数据库在线备份副本执行 V10 后，记录数、正时长数和总时长均不变，旧非出席状态归零；290 个成员/快照分组的时长、记录数和计入次数差异为 0，完整性检查为 `ok`。
- 后端 171 项测试、前端 97 项测试、前端类型检查和生产构建全部通过；当前环境未提供浏览器自动化接口，未将真实浏览器回归标记为已完成。

---

## 17. 任务 13：导出审计补全

### 目标

让所有成功导出的业务数据都能在操作日志中追溯到操作人、数据类型、筛选范围、行数和文件名。

### 修改方案

1. 盘点全部业务导出入口，包括统计、培训场次、培训汇总、维修事务、操作日志和自定义导出。
2. 统一导出日志字段和动作类型，在文件成功生成后记录；失败或被校验拒绝的请求不写“导出成功”日志。
3. 操作日志导出采用“先取得导出快照，后写本次导出日志”的顺序，因此本次导出行为从下一次日志查询起可见，并在文档中说明。
4. 记录必要筛选条件和结果行数，但不把完整成员隐私或工作簿内容写入日志详情。
5. 模板下载、协议预览和空白表单下载不计为业务数据导出，避免日志噪声。

### 验收标准

- 每一种业务数据导出成功后都新增一条可读的操作日志。
- 日志中的角色、操作人、导出类型、筛选范围和行数与实际文件一致。
- 导出失败、权限拒绝和模板下载不会产生误导性的成功日志。

### 验证

- 为每个导出入口增加成功、失败和权限场景测试。
- 打开生成的 Excel 核对数据，并在操作日志页核对对应记录。

### 预计涉及

- `backend/src/main/java/com/ca/attendance/stats/StatsService.java`
- `backend/src/main/java/com/ca/attendance/training/TrainingService.java`
- `backend/src/main/java/com/ca/attendance/repair/RepairCaseService.java`
- `backend/src/main/java/com/ca/attendance/log/OperationLogQueryService.java`
- 导出控制器与相关测试

### 完成记录

- 新增统一 `EXPORT_DATA` 审计契约，值班统计、培训名单、培训汇总、维修事务、操作日志和自定义导出都会记录操作人、角色、导出类型、必要筛选、数据行数和文件名。
- 所有审计均在 Excel 成功生成后写入；权限拒绝、参数校验失败和生成失败不会产生误导性的成功日志。
- 操作日志导出已固定为先生成快照、再写本次导出日志；导入模板、排班模板和维修协议预览不记业务导出日志。
- 统计与操作日志导出返回文件名、字节和行数的统一结果，响应下载文件名与审计文件名使用同一数据源；培训汇总的默认查询范围也已与文件名和审计范围对齐。
- 操作日志页已补全导出动作、对象、角色、筛选条件和行数等中文显示；使用说明已记录审计边界与日志快照规则。
- 新增导出审计集成测试，逐一打开实际 Excel 核对行数，并覆盖六类导出的成功、失败、越权及非业务导出排除场景。
- 后端 175 项测试、前端 98 项测试、前端类型检查和生产构建全部通过。

---

## 18. 任务 14：维修事务增量加载

### 目标

保留当前“进行中 / 已完成 / 已取消”三栏看板和既有组件风格，同时避免维修数据继续增长后一次加载全部记录、DOM 数量过大和页面卡顿。

### 修改方案

1. 新增分页查询契约，每个状态栏独立返回 `items / total / page / pageSize / hasMore`，默认每栏加载 30 条。
2. 三栏分别维护加载、失败、总数和“加载更多”状态，不使用一个总页码强行切割三个状态栏。
3. 关键词、日期和其他筛选变化时取消旧请求并重置三个栏目的分页，防止旧响应覆盖新筛选结果。
4. 新建、编辑、删除后只刷新受影响的栏目和计数，保持当前滚动位置与交互连续性。
5. Excel 导出继续按完整筛选结果生成，不受页面已加载条数影响。

### 验收标准

- 首次进入页面不会把全部维修记录渲染到 DOM，三栏数量与数据库一致。
- 每栏“加载更多”只追加对应状态记录，不重复、不跳项，筛选和编辑后结果正确。
- 1,000 条维修事务规模下页面保持流畅，完整导出的记录数不受分页影响。

### 验证

- 增加分页边界、筛选、并发请求取消和 CRUD 后刷新测试。
- 使用大数据测试库测量接口耗时、首次 DOM 数量和连续加载体验。
- 完成桌面宽屏与窄屏浏览器验收。

### 预计涉及

- `backend/src/main/java/com/ca/attendance/repair/RepairCaseService.java`
- 维修查询控制器、响应模型和集成测试
- `frontend/src` 中维修页面、状态栏组件和 API 客户端

### 完成记录

- 维修查询接口已改为按单一状态分页返回 `items / total / page / pageSize / hasMore`，每栏默认 30 条、单次最多 100 条；完整 Excel 导出仍走不分页查询。
- 前端新增独立维修看板状态管理，进行中、已完成和已取消三栏分别维护页码、总数、加载、失败和“加载更多”状态。
- 关键词和日期筛选会取消过期请求并重置三栏，响应版本校验可防止慢响应覆盖新结果；编辑、创建和删除仅刷新受影响栏目，并保留已经展开的页数。
- 页面已补齐单栏错误重试、加载进度和已加载数量反馈，三栏首屏最多渲染 90 条维修卡片。
- 新增后端分页集成测试和前端状态、页面测试；1,000 条维修事务下验证首栏仅返回 30 条、分页无重复且完整导出仍包含 1,000 条记录。
- 后端 178 项测试、前端 102 项测试、前端类型检查和生产构建全部通过；当前环境无可用浏览器自动化通道，未把桌面宽屏与窄屏人工观感写作已验收。

---

## 19. 任务 15：账号与密码校验统一

### 目标

统一成员新建、Excel 导入、初始化、登录后改密和管理员重置密码的校验规则，避免同一数据在不同入口得到不同结果。

### 修改方案

1. 建立后端统一校验策略：新建或导入的成员学号为 6 至 32 位纯数字，姓名去除首尾空白后为 1 至 64 个字符，密码为 6 至 64 个字符。
2. 既有合法账号不做强制迁移；旧账号只有在修改相关信息时才应用新规则，避免升级时直接阻断登录。
3. 默认以学号重置密码时同样经过密码规则；不满足规则的历史账号必须由管理员输入合规的新密码。
4. 前端补齐 `minlength`、`maxlength`、输入模式和行内错误，但后端仍作为最终校验边界。
5. Excel 导入按行返回明确错误位置和原因，合法行与非法行的处理方式保持现有原子性约定。
6. 对学院、联系方式、年级、备注等字段补齐与数据库容量一致的最大长度，防止异常超长输入进入持久层。

### 验收标准

- 单条新建与 Excel 导入对同一成员数据给出一致结果。
- 改密、管理员重置和默认重置使用同一密码长度边界，不会产生无法登录的新密码。
- 非法字符、纯空白名称、超长字段和边界长度均返回明确的 `400` 错误，既有账号仍可正常登录。

### 验证

- 增加最小值、最大值、越界值、空白、非数字账号和中文字段长度测试。
- 增加成员表单、导入错误展示、改密和重置密码的前端测试与浏览器回归。

### 预计涉及

- 后端统一输入校验类及用户、初始化、认证服务
- 成员导入服务和相关集成测试
- 前端成员、初始化、登录/改密表单及 API 错误展示

### 完成记录

- 新增统一后端输入策略，新建及导入账号统一为 6 至 32 位纯数字，姓名为 1 至 64 个字符，初始化、个人改密、管理员手动重置密码统一为 6 至 64 个字符。
- 成员新增、编辑、个人资料、Excel 导入、首次初始化、个人改密、管理员重置及相关操作原因均已接入同一边界；联系方式、学院、年级和 QQ 分别限制为 64、128、16 和 32 个字符。
- 既有历史账号无需迁移即可继续登录和通过 Excel 更新资料；历史账号不满足当前学号规则时，默认密码重置会被拒绝，并要求管理员手动输入合规新密码。
- Excel 导入会标明具体错误行，任意行非法时整份文件不写入；成员导入弹窗会保留后端行号错误，重新选择文件后清除旧错误。
- 成员、初始化、首次改密、个人改密和管理员重置弹窗均补齐输入属性、字段级错误、错误播报及首个错误字段聚焦；后端仍是最终校验边界。
- 后端 189 项测试、前端 113 项测试、前端类型检查和生产构建全部通过；当前环境无可用浏览器自动化通道，未把可视化浏览器验收写作已完成。

---

## 20. 本轮检查点

### 检查点 A：任务 9 至任务 12

- 备份与恢复闭环通过，且安全限制没有退化。
- 成员删除不会破坏任何历史记录。
- 超过 500 条的批量审核无遗漏且可回滚。
- 培训时长在数据库、接口、页面、统计和导出中口径一致。
- 后端完整测试通过，并完成隔离数据库迁移演练。

### 检查点 B：任务 13 至任务 15

- 所有业务导出均有准确审计记录。
- 维修三栏在大数据量下采用独立增量加载，筛选与导出保持正确。
- 所有账号与密码入口执行统一校验。
- 前端单元测试和生产构建通过；核心页面浏览器验收需在可用的浏览器自动化环境中另行执行。

### 最终回归

1. 运行后端、前端、性能工具和桌面端的现有自动化测试。
2. 执行签到、审核、成员、排班、培训、维修、统计、导出、日志、备份和恢复的完整业务烟测。
3. 使用正式数据库副本验证迁移兼容性，确认正式数据库本身未被测试修改。
4. 检查 Git 范围不包含数据库、备份、导出文件、测试截图、凭据、本机配置或 `tasks/`。
5. 汇报每项验收结果和剩余风险，等待用户决定是否提交、打包和发布。

---

## 21. 维修与培训前端重构计划

### 21.1 改造目标

本轮只重构维修事务与培训记录的页面承载方式，不改变既有业务权限、数据口径、协议内容、导入导出规则和淡蓝色后台风格。

- 维修页面从“三个状态同时铺开”改为“单状态工作区”：进行中强调处理，已完成和已取消作为历史档案查询。
- 培训页面从“场次和名单全部展开”改为“场次目录 + 当前场次详情”：场次与参与名单分别分页，页面不随数据量无限增长。
- 桌面端保持高密度工作台体验；窄屏下一次只呈现一个明确上下文。
- 保留任务 14 已完成的维修分页、过期请求取消、状态计数和局部刷新能力，仅替换页面展示模型。
- 前端按功能拆分组件和状态，不继续扩大现有 `TrainingPage.vue` 与 `RepairsPage.vue`。

### 21.2 已确认的架构决策

1. **维修采用混合布局。** 进行中使用卡片网格；已完成和已取消使用紧凑表格及服务端分页，不再使用三列看板。
2. **培训采用双栏工作区。** 桌面端左侧为分页场次目录，右侧为当前场次详情和分页名单；两侧有独立搜索语义。
3. **移动端不照搬桌面双栏。** 维修使用状态分段切换；培训使用当前场次选择器和场次抽屉，参与名单采用紧凑两行记录。
4. **页面状态进入 URL。** 至少同步维修状态、页码与筛选，以及培训场次 ID、场次页码、名单页码和关键词；刷新、前进和后退可恢复上下文。
5. **分页优先于虚拟列表。** 本地系统规模可通过 20 至 50 条的服务端分页稳定承载，无需引入新的虚拟滚动依赖。
6. **详情使用抽屉。** 维修卡片和历史表格共享同一详情抽屉；现有协议预览和编辑弹窗继续复用。
7. **权限保持不变。** 部长、会长和管理员的维修查看与操作权限，以及会长和管理员的培训权限沿用现有规则。

### 21.3 明确不做的内容

- 不修改数据库表结构，也不清空正式数据库。
- 不修改培训时长统计规则、维修状态枚举或协议正文。
- 不引入新的全局状态框架或大型 UI 组件库。
- 不把维修历史数据预加载到前端，也不为分页后的长列表额外实现虚拟滚动。
- 未收到明确要求前，不打包、不提交、不推送 GitHub、不发布 Release。

---

## 22. 任务 16：培训分页接口

### 目标

为培训场次目录与单场参与名单提供稳定的服务端分页能力，使前端无需一次取得和渲染全部数据。

### 修改方案

1. 新增培训场次分页响应，统一返回 `items / total / page / pageSize / hasMore`；保留关键词和日期筛选，默认每页 20 场，单页上限 100 场。
2. 参与名单接口增加 `keyword / page / pageSize`，支持按姓名、学号和备注查询；默认每页 30 人，单页上限 100 人。
3. 场次查询中的参与人数和累计时长继续返回完整聚合值，不受名单当前页影响。
4. 保持培训导出接口按完整筛选结果导出，不受页面分页影响。
5. 为兼容当前前端，短期保留旧的非分页接口；任务 17 完成调用迁移后再评估移除，避免一次改动同时跨越前后端。

### 验收标准

- 200 个培训场次和单场 3,000 人时，页面接口只返回请求页的数据。
- 参与名单搜索、总数、页码和聚合统计准确，翻页不重复、不漏项。
- 导出名单和导出统计仍包含完整数据。

### 验证

- 后端增加场次分页、名单分页、边界页、非法页码、关键词筛选和大数据量测试。
- 使用实际生成的 Excel 核对分页不会影响导出行数。

### 完成记录

- 新增 `/api/trainings/page` 与 `/api/trainings/{id}/participants/page`，统一返回 `items / total / page / pageSize / hasMore`。
- 场次默认每页 20 条、名单默认每页 30 条，单页均限制为最多 100 条；页码越界会稳定返回空页。
- 名单搜索覆盖姓名、学号和备注，并继续保持主讲人优先排序；场次参与人数和累计时长仍按完整名单聚合。
- 当前前端尚未迁移到分页接口，因此暂时保留原有非分页接口，避免任务 17 开始前产生功能回归；导出仍使用完整数据集。
- 新增独立临时数据库集成测试，覆盖 205 个场次与单场 3,000 人的数据规模、分页边界、搜索、排序和权限矩阵。
- 后端全量测试共 193 项，`failures=0`、`errors=0`、`skipped=0`；`git diff --check` 通过。

### 预计涉及

- `backend/src/main/java/com/ca/attendance/training/TrainingController.java`
- `backend/src/main/java/com/ca/attendance/training/TrainingService.java`
- 培训接口响应模型与集成测试

### 依赖

- 无。

---

## 23. 任务 17：培训工作区状态

### 目标

在页面重排前抽离培训的数据编排，使场次分页、名单分页、筛选、选择和请求竞态可独立测试。

### 修改方案

1. 新建 `useTrainingWorkspace`，分别维护场次页与名单页的 `items / total / page / loading / error`。
2. 场次切换时立即清空旧名单或显示骨架状态，并使用 `AbortController` 与请求版本阻止旧响应覆盖新场次。
3. 新增、编辑、归档培训后只刷新场次目录和当前场次；参与人增删改或导入后只刷新名单与当前场次聚合统计。
4. 将 `sessionId / sessionPage / participantPage / keyword / from / to` 与路由查询参数双向同步。
5. 为读取失败提供对应区域的重试入口，不用全局提示替代页面状态。

### 验收标准

- 快速连续切换两个培训场次时，标题、统计和参与名单始终属于同一场次。
- 刷新页面、浏览器前进或后退后能恢复当前场次、筛选和页码。
- 单一区域请求失败不会清空另一侧的可用数据。

### 验证

- 前端单元测试覆盖迟到响应、取消请求、失败重试、空页回退和 URL 恢复。

### 完成记录

- 新增 `useTrainingWorkspace`，分别维护场次和名单的 `items / total / page / pageSize / hasMore / loading / error`，页面不再直接编排两级请求。
- 新增培训分页 API 适配器，现有培训页面已迁移到任务 16 的场次与名单分页接口；场次每页 20 条、名单每页 30 条。
- 场次与名单请求均使用独立 `AbortController` 和请求版本，快速筛选或切换场次时会取消旧请求并忽略迟到响应；切换场次会立即清空旧名单。
- `sessionId / sessionPage / participantPage / keyword / from / to` 已与路由查询参数同步，支持刷新和浏览器前进、后退恢复；非法页码和删除后的空页会回退到有效页。
- 场次和名单具备独立加载、错误与重试反馈；名单失败不会清空场次数据，参与记录变更只刷新当前名单和场次聚合。
- 新增状态层与页面接线测试，覆盖场次和名单请求取消、迟到响应、URL 恢复、区域重试、空页回退及分页响应渲染。
- 前端全量测试、类型检查和生产构建通过；真实后端冒烟返回 51 场培训中的 20 条第一页数据，名单分页契约响应正常。
- 浏览器容器在新开 `localhost` 页面时被客户端策略拦截，因此本轮未完成可视截图验收；任务 18 的页面重排将继续进行浏览器视觉验证。

### 预计涉及

- `frontend/src/features/training/useTrainingWorkspace.ts`
- `frontend/src/features/training/trainingTypes.ts`
- 对应状态测试

### 依赖

- 任务 16。

---

## 24. 任务 18：培训页面重排

### 目标

将培训页改造成稳定的“场次目录 + 当前场次详情”工作区，解决场次多、名单长和窄屏页面无限增长的问题。

### 修改方案

1. 桌面端工作区高度使用视口约束，左侧场次目录和右侧名单区域各自滚动；页面标题、筛选和详情头不随名单无限延长。
2. 左侧场次目录显示日期、标题、主讲人和人数，底部提供分页；长标题最多两行。
3. 右侧将标题、时间、地点、主讲人、人数和累计时长合并为紧凑详情头。
4. 参与名单增加独立搜索与分页；保留“新增记录”为主操作，“导入名单”为次级操作，导出名单、编辑和归档收入更多菜单。
5. 移动端将场次目录放入抽屉；主页面只显示当前场次。名单使用紧凑两行记录，每页 20 人，备注按需展开。
6. 为场次目录、详情头、名单和场次抽屉建立独立组件，页面只负责组合和弹窗编排。

### 验收标准

- 200 个场次、单场 3,000 人时，页面高度不随总数据量增加，首屏 DOM 数量受分页限制。
- `1440×900`、`1024×768`、`768×1024` 和 `390×844` 下无横向溢出、遮挡或双滚动混乱。
- 移动端无需先滚过场次目录才能查看当前场次名单。

### 验证

- 组件测试覆盖场次选择、分页、更多菜单、空数据、加载和失败状态。
- Playwright 截图比较桌面、平板和手机布局，并检查页面与工作区滚动范围。

### 预计涉及

- `frontend/src/pages/admin/TrainingPage.vue`
- `frontend/src/features/training/TrainingSessionList.vue`
- `frontend/src/features/training/TrainingSessionHeader.vue`
- `frontend/src/features/training/TrainingParticipantList.vue`
- `frontend/src/features/training/TrainingSessionDrawer.vue`
- 培训专项样式与测试

### 依赖

- 任务 17。

### 完成记录

- 培训页已改为受控高度双栏工作区：桌面端左侧场次目录、右侧当前场次详情和参与名单各自滚动，页面 DOM 规模受每页 20 条限制。
- 场次目录、场次详情头、参与名单和移动场次抽屉已拆为独立组件；`TrainingPage.vue` 缩减到 517 行，没有形成新的超大单文件组件。
- 参与名单新增独立关键词搜索和分页；新增记录、导入名单保留为显式操作，导出、编辑和归档集中到可键盘操作的更多菜单。
- `820px` 以下切换为当前场次单栏，并通过移动抽屉选择场次；抽屉包含固定分页、焦点管理和背景滚动锁，关闭后焦点返回触发按钮。
- `1100px` 以下名单改为两行紧凑记录，消除了 1024 宽度下编辑、删除操作横向溢出。
- 新增组件与状态测试，覆盖选择、分页、搜索、更多菜单、加载、失败、空数据、抽屉关闭和滚动锁；前端完整 `45` 个测试文件、`127` 项测试通过，类型检查和生产构建通过。
- 使用真实规模数据库在 `1440×900`、`1024×768`、`768×1024`、`390×844` 四档视口完成浏览器检查：无页面横向溢出，目录与名单滚动边界正确，移动抽屉分页完整可见，控制台无错误或警告。

---

## 25. 任务 19：维修工作区状态

### 目标

在复用现有维修分页接口和请求保护的基础上，把三栏并行状态改为单状态工作区状态。

### 修改方案

1. 将 `useRepairBoard` 调整为 `useRepairWorkspace`，只维护当前状态的分页数据，同时缓存三个状态的总数。
2. 默认进入“进行中”；状态切换、关键词、日期和页码同步到 URL。
3. 新建或编辑导致状态变化时，刷新原状态和目标状态计数，并在当前视图中保持合理位置。
4. 进行中与历史状态共用同一查询契约，但分别暴露卡片和表格需要的展示数据。
5. 保留现有过期请求取消、响应版本校验、单区域错误和重试能力。

### 验收标准

- 首次进入只请求当前状态列表，不再同时请求并渲染三个状态各 30 条。
- 三个状态数量准确；切换、筛选、翻页和 CRUD 后不会显示旧状态的数据。
- 刷新和浏览器前进、后退可以恢复当前维修状态和页码。

### 验证

- 前端状态测试覆盖状态切换、迟到响应、空页回退、跨状态编辑和 URL 恢复。
- 保留并改写原有 `useRepairBoard` 的分页与取消请求测试，避免已有能力退化。

### 预计涉及

- `frontend/src/features/repairs/useRepairWorkspace.ts`
- `frontend/src/features/repairs/repairTypes.ts`
- 维修工作区状态测试

### 依赖

- 无；复用现有维修分页接口。

### 完成记录

- 新增 `useRepairWorkspace`，维修页只维护当前状态的一页数据；首次进入仅请求“进行中”，切换状态后只请求目标状态，不再并行拉取和渲染三栏记录。
- 维修分页响应新增筛选后的 `statusCounts`，一次请求同时返回进行中、已完成、已取消三个准确计数；列表总数与计数在同一个只读事务快照中读取。
- 状态、页码、关键词和日期筛选已同步到 URL；初始化与查询恢复使用替换，用户主动切换和翻页进入浏览器历史，前进、后退可恢复原状态和页码。
- 保留请求取消与响应版本检查，旧状态或旧筛选的迟到响应不会覆盖当前列表；记录减少导致空页时自动回退到最后有效页。
- 新建、编辑、跨状态修改和移入回收站后重新加载当前页，并通过同一响应刷新三个状态计数；旧 `useRepairBoard` 三栏状态实现及测试已删除。
- 页面暂时保留现有维修卡片、协议、电话遮罩和编辑弹窗，只增加状态切换与分页基础布局；进行中卡片和历史表格的视觉分化留给任务 20。
- 新增和改写状态及页面测试；前端 `45` 个测试文件、`131` 项测试通过，类型检查和生产构建通过，后端 `39` 个测试文件、`193` 项测试通过。
- 使用真实数据库验证进行中 `19` 项、已完成 `177` 项、已取消 `27` 项：状态切换、第二页、浏览器回退和计数均正确，页面无横向溢出，控制台无应用错误或警告。

---

## 26. 任务 20：维修页面重排

### 目标

将维修页改为“进行中工作队列 + 历史档案”，解决已完成记录长期累积导致的三列失衡。

### 修改方案

1. 页面顶部增加 `进行中 / 已完成 / 已取消` 分段控件及总数，一次只显示一个状态。
2. 进行中采用两列紧凑卡片；优先展示设备、故障、处理时长和负责人，联系人、受理时间降为次级信息。
3. 已完成和已取消采用分页表格，展示编号、设备、联系人、负责人、完成或取消时间及查看操作。
4. 点击卡片或历史行打开共享详情抽屉；协议预览、编辑和删除从详情中可达，同时保留必要的快捷操作。
5. 搜索框常驻，日期等条件折叠进筛选面板；移动端一次只显示当前状态，历史记录使用紧凑两行列表。
6. 删除现有三列布局专属样式，复用现有表格、分页、分段控件和抽屉视觉规范。

### 验收标准

- “进行中 5 条、已完成 1,000 条、已取消 30 条”时，默认页面只渲染进行中记录，历史页每页不超过 30 条。
- 桌面端与手机端切换状态无需长距离滚动，主要操作位置稳定。
- 手机号默认遮罩、长时间未处理提醒、协议预览和权限控制均不退化。

### 验证

- 组件测试覆盖三种状态布局、分页、详情抽屉、权限差异和跨状态编辑。
- Playwright 使用不同比例数据验证桌面和移动端观感及滚动行为。

### 预计涉及

- `frontend/src/pages/admin/RepairsPage.vue`
- `frontend/src/features/repairs/RepairStatusTabs.vue`
- `frontend/src/features/repairs/ActiveRepairGrid.vue`
- `frontend/src/features/repairs/RepairHistoryTable.vue`
- `frontend/src/features/repairs/RepairDetailDrawer.vue`
- 维修专项样式与测试

### 依赖

- 任务 19。

### 完成记录

- 新增 `RepairStatusTabs`、`ActiveRepairGrid`、`RepairHistoryTable` 和 `RepairDetailDrawer` 四个独立组件；维修页本体降至约 `600` 行，只负责数据、路由与弹窗编排。
- 进行中事务改为桌面两列工作队列，优先展示设备、故障、处理时长和负责人；超过七天继续使用明确的长时间未处理标识，手机号默认保持遮罩。
- 已完成和已取消改为服务端分页历史表格，每页最多 `30` 条；手机端重排为约 `67px` 高的紧凑两行记录，不再纵向展开全部次级信息。
- 卡片和历史行共用详情抽屉，支持键盘打开、Escape 关闭、焦点约束与页面滚动锁定；协议预览、编辑和移入回收站按原角色权限从详情中可达。
- 搜索框常驻，日期筛选折叠；旧三栏看板及卡片专属样式已从通用样式层删除，维修视觉规则集中到 `repair-workspace.css`。
- 修正已取消事务的流程时长截止点：优先使用完成时间，否则使用最后更新时间，不再随当前日期继续增长。
- 前端 `46` 个测试文件、`136` 项测试全部通过，类型检查和生产构建通过；后端 JAR 重新打包成功。
- 使用独立数据库副本验证真实比例数据：进行中 `19` 条、已完成 `177` 条、已取消 `27` 条；桌面端两列卡片、历史每页 `30` 条、共享详情抽屉和手机两行记录均正常，无横向溢出或控制台错误。

---

## 27. 任务 21：两页交互与表单收口

### 目标

在新布局稳定后统一修复两页已有的异步反馈、导入状态和表单可用性问题。

### 修改方案

1. 为保存、删除、导入和导出建立独立 `pending` 状态；请求期间只锁定对应按钮并显示加载反馈，防止重复提交。
2. 培训导入弹窗每次打开和关闭都重置文件；显示文件名、大小和移除操作，导入失败后保留文件供修正重试。
3. 维修协议预览使用请求版本或 `AbortController`，关闭或切换记录时不允许旧响应覆盖新协议。
4. 表单补齐有意义的 `name / autocomplete / type / inputmode`，提交错误时在字段旁显示原因并聚焦第一个错误字段。
5. 维修三个确认项统一语义：在不改变当前业务规则的前提下改为事实记录式文案，避免被理解为必选提交条件。
6. 编辑弹窗有未保存修改时，关闭、切换页面或切换记录前给出应用内确认。

### 验收标准

- 快速点击不会产生重复培训、参与记录或维修事务。
- 导入弹窗不会静默保留上次文件，用户始终知道即将导入哪个文件。
- 协议与名单不会因迟到响应显示到错误对象下。
- 键盘用户可以完成两页核心操作，错误位置明确。

### 验证

- 增加重复点击、导入重开、请求竞态、失败重试和未保存离开测试。
- 运行键盘操作和可访问名称检查。

### 预计涉及

- 培训与维修弹窗组件
- `frontend/src/shared/composables/useAsyncTask.ts` 或功能内异步状态封装
- 相关页面与组件测试

### 依赖

- 任务 18、任务 20。

### 完成记录

- 保存培训、保存参与记录、归档、删除、导入、模板下载、名单导出、统计导出、维修保存与删除均使用独立操作锁；快速重复点击不会重复发出同类请求，其他区域仍可独立使用。
- 培训导入弹窗显示文件名、大小和移除操作；导入失败保留当前文件，关闭或重新打开会清空旧文件。
- 培训场次、参与记录和维修事务编辑器已拆为独立组件，补齐表单名称、输入类型、自动填充提示、行内错误和首个错误聚焦；维修确认项改为事实记录式文案。
- 两页编辑弹窗关闭、切换页面时均有未保存修改确认；维修协议预览加入请求版本保护，旧响应不能覆盖当前记录。
- 前端全量 `55` 个测试文件、`149` 项测试通过，类型检查和生产构建通过；后端 `192` 项测试通过。
- 使用正式数据库的临时副本完成真实浏览器验收：桌面与 `390px` 窄屏无横向溢出，弹窗页脚可见、首焦点正确，控制台无错误或警告；临时服务和数据库副本已清理。

---

## 28. 任务 22：大数据视觉与回归验收

### 目标

用接近真实运营规模的数据完成性能、观感、功能和桌面端回归，确认重构不仅在小数据演示中成立。

### 测试数据规模

- 培训场次至少 200 场，其中单场参与名单分别覆盖 0、1、30、72 和 3,000 人。
- 维修事务至少 1,100 条，包括进行中 8 条、已完成 1,050 条、已取消 42 条。
- 包含长标题、长故障描述、空备注、未关联成员、历史负责人和各种权限角色。
- 全部写入隔离测试数据库，不修改正式数据库。

### 验收范围

1. 验证培训场次分页、名单分页、搜索、导入、导出和 CRUD。
2. 验证维修状态切换、历史分页、详情、协议、编辑、删除和完整导出。
3. 验证 `1440×900`、`1024×768`、`768×1024`、`390×844`，以及桌面应用窗口缩放。
4. 检查横向溢出、页面高度、首屏 DOM 数量、焦点顺序、控制台错误和加载反馈。
5. 运行后端完整测试、前端完整测试、类型检查和生产构建。

### 通过标准

- 大数据场景下页面高度由当前页内容决定，不由完整历史数据量决定。
- 任一列表首屏渲染不超过其分页上限，筛选和翻页没有旧响应覆盖。
- 桌面和窄屏无错位、遮挡、不可操作区域或非预期页面横向滚动。
- 培训导出、维修导出、协议预览、权限控制和既有统计口径全部通过回归。
- 形成桌面、平板和手机截图及测试结果摘要，等待用户决定后续提交或发布。

### 依赖

- 任务 21。

### 完成记录

- 新增可重复执行的大数据种子、接口验收、视觉验收与桌面缩放冒烟脚本；全部使用系统临时目录中的独立 `APP_ROOT` 和 SQLite，正式数据库时间戳未变化。
- 隔离数据实际包含 `500` 名成员、`10,000` 条签到、`200` 场培训、`5,053` 条培训参与记录、`1,100` 条维修和 `5,000` 条日志；单场名单覆盖 `0 / 1 / 30 / 72 / 3,000` 人，维修状态精确为 `8 / 1,050 / 42`。
- 培训场次与 3,000 人名单首尾分页、搜索、未关联成员、CRUD、Excel 导入、名单导出和统计导出通过；维修三状态分页、末页、协议预览、CRUD、回收站恢复、完整导出和部长权限边界通过。
- 大数据验收发现并修复维修历史区在 `1024px` 的页面横向溢出，以及手机历史卡片中设备与完成时间字段粘连；历史档案在 `1100px` 以下切换为紧凑卡片布局。
- `1440×900`、`1024×768`、`768×1024`、`390×844` 共 `12` 张截图通过复核；四档页面横向溢出与可见单元格碰撞均为 `0`，培训、维修首屏分别不超过 `20 / 30` 条，弹窗首焦点正确，控制台无错误或警告。
- Electron 实际完成 `1080×720` 与 `1440×900` 窗口切换，两个尺寸内容横向溢出均为 `0`；冒烟逻辑允许 Windows 窗口边框产生的最多 `2px` 尺寸差异。
- 本机本轮样本中，培训分页 P95 `4.96ms`、3,000 人名单分页 P95 `5.87ms`、维修进行中分页 P95 `4.19ms`、维修历史分页 P95 `28.26ms`；3,000 人名单导出约 `0.66s`，1,100 条维修导出约 `0.86s`。
- 前端 `55` 个测试文件、`149` 项测试，后端 `193` 项测试，脚本 `14` 项测试和桌面 `17` 项测试全部通过；类型检查、生产构建和后端打包通过。
- 最终报告保存在系统临时目录 `ca-attendance-large-data-task22.json`，截图保存在同目录下的 `ca-attendance-large-data-task22-screenshots`；未提交、未推送、未打包桌面发行版。

---

## 29. 本轮执行顺序与检查点

### 第一阶段：培训数据承载

1. 任务 16：培训分页接口。
2. 任务 17：培训工作区状态。

#### 检查点 C

- 后端培训分页测试通过。
- 场次与名单竞态测试通过。
- 原有培训 CRUD、导入、导出和统计测试无回归。

### 第二阶段：培训页面

3. 任务 18：培训页面重排。

#### 检查点 D

- 培训桌面端和手机端均不再因总记录数无限增长。
- 用户确认培训页面的排版方向后，再进入维修视觉实现。

### 第三阶段：维修页面

4. 任务 19：维修工作区状态。
5. 任务 20：维修页面重排。

#### 检查点 E

- 维修默认只显示进行中；历史状态分页正确。
- 原有分页、导出、隐私遮罩、协议和权限能力无回归。

### 第四阶段：交互与最终验收

6. 任务 21：两页交互与表单收口。
7. 任务 22：大数据视觉与回归验收。

#### 检查点 F

- 完整自动化测试、类型检查、生产构建和浏览器验收全部通过。
- Git 范围不包含数据库、备份、导出文件、截图、凭据、本机配置或 `tasks/`。
- 汇报改动与剩余风险，等待用户明确决定是否打包、提交、推送或发布。

## 30. 风险与控制

| 风险 | 影响 | 控制方式 |
| --- | --- | --- |
| 培训分页接口改变现有前端契约 | 高 | 先完成后端契约与测试，再切换前端；同一提交阶段保持可运行 |
| URL 状态与组件内部状态循环更新 | 中 | 建立单向解析与规范化函数，增加前进/后退测试 |
| 工作区内部滚动形成双滚动困扰 | 中 | 仅桌面端启用受控高度；平板与手机恢复文档流并分页 |
| 维修跨状态编辑后计数或页码失真 | 中 | 原状态与目标状态同时刷新，空页自动回退上一页 |
| 新组件数量过多造成抽象负担 | 低 | 只拆具有独立状态或视觉职责的组件，不创建纯转发包装器 |
| 大数据测试误触正式数据库 | 高 | 使用独立 `APP_ROOT` 和一次性 SQLite 数据库，测试结束后核对正式库未变化 |

## 31. 规划完成检查

- [x] 任务依赖按接口、状态、页面和验收排序。
- [x] 维修重构复用现有分页与请求保护，不重复建设后端能力。
- [x] 培训分页同时覆盖场次和参与名单。
- [x] 桌面端与移动端采用不同但一致的信息架构。
- [x] 每项任务包含验收标准和验证方式。
- [x] 明确正式数据库、Git、打包和发布边界。
- [x] 用户已确认并完成任务 20。
- [x] 用户已确认并完成任务 21。
- [x] 用户已确认并完成任务 22；全部计划任务已执行完成。
