# AGENTS.md

## 项目定位

本项目是面向计算机协会的 Windows 本地离线管理系统，由以下部分组成：

- `frontend/`：Vue 3、TypeScript、Vite 前端
- `backend/`：Spring Boot 4、Spring JDBC、SQLite 后端
- `desktop/`：Electron 桌面外壳
- `scripts/`：测试、构建和发布脚本
- `docs/`：权限、业务和验收基线文档

开始任务前先阅读 `DEV_STATE.md`。涉及业务规则时，以需求说明书及 `docs/` 中的权限矩阵、状态矩阵和验收矩阵为准。

## 核心约束

- 系统必须保持离线可运行，不得引入依赖互联网的运行时服务。
- 本机服务使用 `127.0.0.1:8080`。
- 远程管理入口使用 `127.0.0.1:8081`，不得开放签到台。
- 远程入口只允许会长和管理员登录。
- SQLite 数据库位于应用根目录对应的数据目录，修改结构必须通过迁移完成。
- 不得在代码、文档、测试或提交记录中写入密码、Token、私钥或真实成员数据。
- 不得提交数据库、备份、导出、日志、运行时、安装包、测试截图、`tasks/` 或 `local-config.bat`。

## 修改原则

- 修改前先检查 `git status` 和相关代码，保留用户已有改动。
- 不得撤销、覆盖或格式化与当前任务无关的文件。
- 优先沿用现有模块、命名和接口，不进行无关重构。
- 后端按领域拆分查询、事务、导入导出和文档生成职责。
- 避免重新形成超大 Service 或 Vue 单文件组件。
- 涉及权限时必须同时检查后端授权、前端导航和直接 API 访问。
- 涉及删除、批量修改、审核、恢复等操作时必须保证事务和审计一致性。
- 新增导入功能必须限制文件大小、数据行数，并保证校验失败时整批不写入。

## 子代理使用

- 全局自定义子代理 `luna_worker` 适合边界明确、可重复、影响范围较小的任务，例如定向检索、单文件机械修改、运行指定测试和核对生成结果。
- 委派给 `luna_worker` 时必须说明具体目标、允许修改的文件或目录、禁止触碰的内容、验证命令和期望的汇报格式。
- `luna_worker` 应独立完成任务，在可行时验证结果，并简洁返回结果、相关文件路径、执行的验证和重要注意事项。
- 主代理必须等待并检查 `luna_worker` 的结果，不得未经复核直接采用其修改或结论。
- 跨模块设计、权限与事务调整、数据库迁移、安全决策、大范围重构、Git 提交、打包和发布由主代理负责，除非用户明确要求将其中某个边界清楚的子任务委派出去。
- 委派不会扩大权限；未经用户明确要求，`luna_worker` 同样不得打包、提交、推送、创建 PR 或发布 Release。
- 推荐委派提示词：`请使用 luna_worker 子代理完成以下任务：<具体任务>。仅允许修改 <文件范围>，不要改动其他内容；完成后运行 <验证命令>，并汇报结果、相关文件和注意事项。`

## 前端规范

- 保持现有淡蓝色视觉体系和统一组件风格。
- 后台、登录页和签到台应属于同一设计语言，但信息密度可以不同。
- 业务表格、表单和弹窗必须支持常见桌面分辨率及浏览器缩放。
- 输入框应提供真实 `label`，错误信息应清晰且可定位。
- 删除和危险操作使用应用内确认弹窗，不使用原生 `prompt` 或 `confirm`。
- 使用现有 Lucide 图标，不手写可被图标库替代的 SVG。
- `backend/src/main/resources/static/` 是前端构建输出，不直接手工修改其中的哈希资源；应修改 `frontend/src/` 后运行前端构建。

## 后端规范

- Java 版本为 21。
- Controller 只负责请求映射、参数转换和响应。
- Service 负责权限、事务和业务编排。
- 查询、Excel、协议生成等较大职责应拆为独立组件。
- 所有写操作应检查实际影响行数。
- 业务数据变更与审计日志应位于同一事务中。
- 对外异常应转换为稳定、明确的 HTTP 状态和中文错误信息。
- SQLite 测试必须使用隔离的临时 `APP_ROOT`，不得操作真实数据库。

## 验证要求

根据修改范围运行最小相关测试，并在完成前进行对应回归：

- 后端：在 `backend/` 执行 `mvn -q test`
- 前端：在 `frontend/` 执行 `npm test`
- 类型检查：在 `frontend/` 执行 `npm run typecheck`
- 前端构建：在 `frontend/` 执行 `npm run build`
- 桌面端：在 `desktop/` 执行 `npm test`
- 最终检查：执行 `git diff --check`

仅修改后端时不要求无意义地重新打包桌面端，但必须运行相关后端测试。涉及前端布局或交互时，应在真实浏览器中检查桌面、窄屏和缩放场景。

## Git 与发布

- 未经用户明确要求，不得打包、提交、推送、创建 PR 或发布 Release。
- 用户明确要求提交时，先检查提交范围和 `.gitignore`。
- `tasks/` 永远不得提交。
- 发布前按照 `docs/发布检查表.md` 执行完整验证。
- 不得为获得干净工作区而删除或还原用户已有修改。

## 状态记录

完成一个阶段后更新 `DEV_STATE.md`，只记录已经确认的事实：

- 当前完成内容
- 关键设计决策
- 实际测试结果
- 已知问题和未验证范围
- 下一项建议任务

不要在 `AGENTS.md` 中记录临时进度、版本提交号、测试数量或待办清单。
