# AGENTS.md

## 作用范围

本文件适用于整个仓库，只记录长期有效的开发规则。临时进度、版本提交号、测试数量和阶段待办写入 `DEV_STATE.md` 或任务计划，不写入本文件。

项目是面向计算机协会的 Windows 本地离线管理系统：

- `frontend/`：Vue 3、TypeScript、Vite 前端
- `backend/`：Spring Boot 4、Spring JDBC、SQLite 后端
- `desktop/`：Electron 桌面外壳
- `scripts/`：测试、构建和发布脚本
- `docs/`：业务规则、权限、视觉和验收基线

## 开始任务

1. 阅读 `DEV_STATE.md`，了解当前已确认状态和下一步任务。
2. 执行 `git status --short --branch`，保留用户已有改动。
3. 只读取当前任务相关的文档、源码、类型和测试；修改前先找一个现有相似实现。
4. 多文件或跨层行为变更先说明简短执行顺序；用户只要求规划或审阅时，不提前修改代码。
5. 发现文档、代码与当前需求冲突时，明确指出冲突并确认，不静默猜测业务规则。

当前用户要求决定任务范围；需求说明书和对应业务基线决定业务语义；代码与测试用于确认当前实现；`DEV_STATE.md` 只记录进度，不替代业务规则。

## 资料索引

按任务选择性阅读，不要无差别加载全部文档：

| 任务 | 首选依据 |
| --- | --- |
| 总体需求与部署边界 | `计算机协会本地管理系统需求说明书.md` |
| 角色与权限 | `docs/角色权限矩阵.md` |
| 签到、审核与时长 | `docs/签到与时长状态矩阵.md` |
| 业务模块验收 | `docs/业务模块验收矩阵.md` |
| 前端设计与页面观感 | `docs/前端视觉规范.md`、`docs/后台页面视觉验收.md` |
| 桌面端稳定性 | `docs/桌面端稳定性测试记录.md` |
| 数据安全与恢复 | `docs/数据安全演练记录.md` |
| 打包与发布 | `docs/发布检查表.md` |

## 不可突破的边界

- 系统必须离线可运行，不得引入依赖互联网的运行时服务、字体、脚本或资源。
- 本机服务只绑定 `127.0.0.1:8080`；远程管理入口只绑定 `127.0.0.1:8081`。
- 远程入口不得暴露签到台，只允许会长和管理员登录。
- SQLite 数据库位于 `APP_ROOT/data/`；结构变更必须使用迁移，不直接改写既有数据库。
- 开发和测试不得操作已安装桌面端的正式数据库。SQLite 测试必须使用唯一的临时 `APP_ROOT`，完成后关闭临时后端、浏览器和桌面进程。
- 不得在代码、文档、测试、日志或提交记录中写入密码、Token、私钥、Cookie 或真实成员数据。
- 不得提交数据库、备份、导出、日志、运行时、安装包、测试截图、`tasks/`、`plan.md` 或 `local-config.bat`。
- 不得撤销、覆盖、删除或批量格式化与当前任务无关的用户改动。

## 修改原则

- 优先沿用现有领域模块、组件、命名、接口和错误格式，不做无关重构。
- 改动保持边界清楚；发现旁支问题先记录，不顺手扩大任务范围。
- 修复缺陷时先形成可重复证据，优先补充能在修复前失败、修复后通过的回归测试。
- 不为通过测试而降低校验、跳过测试或修改与需求矛盾的断言。
- 新增依赖前确认现有代码和标准库不能合理解决，并评估离线可用性、许可证、体积与安全影响。
- 生成文件只通过对应构建流程更新，不手工编辑哈希产物或安装目录中的资源。

## 权限、数据与事务

- 权限变更必须同时检查后端授权、前端导航/按钮和直接 API 访问，后端权限是最终边界。
- 删除、审核、批量修改、恢复和其他敏感写操作必须保证业务数据与审计日志位于同一事务中。
- 所有写操作检查实际影响行数；失败时不得留下部分写入或虚假成功日志。
- 备份、恢复、清空和批量操作需要考虑并发窗口、原子性和失败后的可恢复性。
- 导入功能必须限制文件大小、数据行数和错误数量，先完整校验，再整批写入；失败时不得部分导入。
- 导出、日志和错误响应不得泄露超出当前角色权限的个人信息或内部细节。

## 前端规范

- 保持现有淡蓝色视觉体系；后台、登录页、初始化页和签到台使用同一设计语言，但按场景控制信息密度。
- 优先复用现有共享组件、设计变量和 Lucide 图标，不手写已有图标，不新增平行样式体系。
- 表单提供真实 `label`、清晰的行内错误和合理焦点；提交失败聚焦首个错误并保留用户输入。
- 删除和危险操作使用应用内确认弹窗，不使用原生 `prompt` 或 `confirm`。
- 页面必须适应常见桌面分辨率、窄屏和浏览器缩放，不允许文字、操作列、弹窗或表格无意义错位溢出。
- 动效服务于状态反馈和层级变化，并尊重 `prefers-reduced-motion`；避免影响录入效率的装饰动画。
- 不添加无意义说明、重复状态和长期占据首屏的低价值信息。
- 按业务功能拆分页面、状态、API 和样式；不得新增或把单个 Vue SFC 推到 2000 行以上，接近失控前主动拆分。
- `backend/src/main/resources/static/` 是前端构建输出。修改 `frontend/src/` 后通过前端构建同步，不直接编辑其中的哈希资源。
- 布局或交互变更必须使用真实浏览器检查桌面、窄屏和缩放场景；关键创建、修改和删除流程要验证最终提交，不只验证弹窗能打开。

## 后端规范

- 使用 Java 21。Controller 只负责映射、参数转换和响应；Service 负责权限、事务和业务编排。
- 查询、Excel、协议生成、备份等较大职责拆为独立组件，避免形成超大 Service。
- 对外异常转换为稳定、明确的 HTTP 状态和中文错误信息，不向前端返回堆栈或数据库细节。
- 日期、时间、结构化数据和 Excel 使用对应类型与解析器，不用脆弱的字符串拼接代替。
- 数据库迁移兼容全新安装与现有数据升级，并补充对应迁移或集成测试。

## 桌面端规范

- 桌面端只是本机服务的受控外壳，不绕过后端权限、事务或数据目录约定。
- 不直接修改已安装目录中的程序或资源；需要复现桌面问题时使用构建产物和隔离 `APP_ROOT`。
- 涉及启动流程时检查全新空库初始化、已有数据库登录、后端启动失败提示和端口冲突。
- 涉及窗口行为时检查缩放、最小尺寸、关闭到托盘、托盘恢复和完全退出；测试结束不得遗留后台 Java/Electron 进程。
- 桌面端协议预览、下载和凭据存储需要同时验证 Electron 环境，不以普通浏览器通过代替桌面验证。

## 子代理使用

- `luna_worker` 只用于边界明确、可重复、影响较小的任务，例如定向检索、单文件机械修改、指定测试和结果核对。
- 委派时写清目标、允许修改范围、禁止事项、验证命令和汇报格式。
- 主代理必须等待并复核子代理的 diff 与验证结果；子代理结论不能未经检查直接采用。
- 跨模块设计、权限与事务、数据库迁移、安全决策、大范围重构、Git 操作、打包和发布默认由主代理负责。
- 委派不扩大权限；未经用户明确要求，子代理同样不得提交、推送、创建 PR、打包或发布。

推荐格式：`请使用 luna_worker 完成 <任务>。仅允许修改 <范围>；不要触碰 <禁止项>；完成后运行 <验证>，并汇报结果、文件和注意事项。`

## 验证矩阵

根据修改范围先运行最小相关测试，再进行必要回归；没有运行的验证必须如实说明。

| 范围 | 命令或检查 |
| --- | --- |
| 后端 | 在 `backend/` 执行 `mvn -q test` |
| 前端单元测试 | 在 `frontend/` 执行 `npm test` |
| 前端类型检查 | 在 `frontend/` 执行 `npm run typecheck` |
| 前端生产构建 | 在 `frontend/` 执行 `npm run build` |
| 桌面端 | 在 `desktop/` 执行 `npm test` |
| 最终文本检查 | 在仓库根目录执行 `git diff --check` |

- 仅修改文档时不运行无意义的全量测试，但仍执行 `git diff --check` 并核对链接、路径和事实。
- 仅修改后端时无需重新打包桌面端，但必须运行相关后端测试。
- 涉及前端布局或交互时，除自动化测试外还要进行真实浏览器视觉与交互检查。
- 涉及桌面启动、窗口、协议预览或打包时，必须进行对应桌面成品验证。
- 发布级全量验证只在用户明确要求打包或发布时，按 `docs/发布检查表.md` 执行。

## Git 与发布

- 未经用户明确要求，不得打包、提交、推送、创建 PR、创建标签或发布 Release。
- 用户明确要求 Git 操作时，先检查 `git status`、提交范围和 `.gitignore`，不得夹带无关文件或敏感数据。
- `tasks/` 永远不得提交；构建产物和运行数据必须继续保持忽略状态。
- 不得为了获得干净工作区而删除、还原或覆盖用户已有修改。
- 发布前执行 `docs/发布检查表.md`；版本、标签、提交、安装包和 Release 必须一致。

## 完成与交接

- 最终汇报应包含：完成内容、相关文件、实际验证、未验证范围和重要注意事项。
- 完成一个实质阶段后更新 `DEV_STATE.md`，删除过时内容，只保留已确认的当前目标、关键决策、测试结果、已知问题和下一步。
- 不在 `DEV_STATE.md` 中记录密码、Token、真实成员数据或未经验证的推测。
