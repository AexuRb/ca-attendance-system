# 本地运行说明

系统面向单台 Windows 电脑运行，两个服务端口都只监听回环地址，不会直接开放局域网端口：`127.0.0.1:8080` 用于本机签到台和完整后台，`127.0.0.1:8081` 用于可选的受限远程管理隧道。

## 1. 选择发行包

日常使用支持 Windows 10/11 x64，不需要安装 Java、MySQL、Node.js 或 Maven，也不需要联网。

### 安装版

运行 `CA-Attendance-System-Setup-3.0.0.exe`，按向导安装。默认目录为：

```text
C:\CAAttendance\app
```

开始菜单和桌面快捷方式会直接启动应用。业务数据保存在 `C:\CAAttendance` 下，不放进 `app` 程序目录。

### 便携版

解压 `CA-Attendance-System-Portable-3.0.0.zip`，保持目录结构不变，双击：

```text
启动管理系统.bat
```

也可以直接运行 `app\CA-Attendance-System.exe`。便携版适合整文件夹复制到另一台电脑。

## 2. 首次初始化

首次启动时，程序会自动创建：

```text
data\attendance.db
```

随后显示“系统首次初始化”，由现场人员填写管理员账号、姓名和密码。系统没有预置公开管理员密码，也不需要执行数据库脚本。

初始化完成后应立即进入“系统 > 设置”配置值班星期和值班时间段，再进入“值班 > 排班”安排部长。

## 3. 启动与退出

桌面应用启动后会自动拉起本机后端并打开签到台。应用运行期间，可同时在本机浏览器访问：

```text
http://127.0.0.1:8080
```

点击桌面窗口右上角关闭按钮后，窗口会隐藏到 Windows 托盘，本机后端和浏览器访问仍会继续运行。点击托盘图标可以重新打开窗口；需要停止服务时，在托盘菜单中选择“完全退出”，程序会先请求后端正常停机，再退出内置 Java。

复制数据、更新程序或关机前，应通过托盘菜单“完全退出”，并确认本机服务已经停止。

### 樱花穿透远程管理

需要远程管理时，在樱花穿透中新建隧道，并将本地目标严格设置为：

```text
主机：127.0.0.1
端口：8081
```

不要把 `8080` 配置到隧道。远程入口只显示后台登录，会长和管理员可以保留原有后台权限；成员、部长、首次初始化、签到台和桌面控制均不能远程使用。即使把本机成员或部长令牌复制到远程请求，后端也会拒绝。

若所选隧道提供 HTTPS 或额外访问认证选项，建议启用；后台账号应使用独立强密码。不需要远程管理时，应在樱花客户端停用隧道。SQLite 数据仍只保存在主机，最多约 10 人使用时应避免多人同时编辑同一条业务记录。

## 4. 目录结构

```text
CAAttendance\
├─ app\                     可替换的程序文件
├─ data\attendance.db       SQLite 主数据库
├─ backups\app\             后台一键备份
├─ exports\                 导出目录
└─ logs\                    desktop.log 与 backend.log
```

更新程序时只替换 `app`；不要删除或覆盖 `data`、`backups`、`exports` 和 `logs`。数据库和备份未加密，请限制无关人员访问该文件夹。

## 5. 备份、恢复与迁移

会长或管理员可以在“系统 > 数据”的“备份与恢复”中执行一键备份，文件保存在：

```text
backups\app
```

备份包含成员、签到、培训、排班、维修、日志和值班设置。只有管理员可以恢复或删除备份；恢复前系统会自动保存当前数据，完成后所有账号需要重新登录。

系统创建备份时不设数据表行数上限，并以文件流方式生成和下载，避免把整个 ZIP 同时放入内存。创建和恢复使用相互独立的容量策略，当前默认字节边界保持一致：ZIP 不超过 128 MiB、单条目未压缩内容不超过 128 MiB、全部未压缩内容不超过 256 MiB，最多 16 个顶层条目，因此当前版本生成的备份仍能由当前版本恢复。新备份发布后会自动清理旧文件，默认最多保留 50 份且合计不超过 5 GiB，并始终至少保留最新一份。源码启动时可在 `local-config.bat` 中调整：

```bat
set "APP_BACKUP_MAX_FILES=50"
set "APP_BACKUP_MAX_TOTAL_BYTES=5368709120"
```

两个值都必须是大于 0 的整数。恢复校验失败不会修改现有数据库。备份列表会忽略刚好被另一管理页面删除的文件，写入或发布失败的临时文件不会出现在可恢复列表中。

整机迁移步骤：

1. 从托盘菜单选择“完全退出”，确认 `127.0.0.1:8080` 已无法访问。
2. 复制整个 `CAAttendance` 文件夹到新电脑。
3. 从新目录启动 `app\CA-Attendance-System.exe`。
4. 登录后台检查成员、排班和最近备份。

不要在应用运行时直接复制 `attendance.db`；运行中的数据请通过后台备份导出。

## 6. 忘记密码

成员密码由会长或管理员在成员页面重置。建议至少保留两个可用的管理员账号，并妥善保存管理员凭据；系统不提供绕过登录的本机密码恢复入口。

## 7. 未签名安装包

当前项目没有代码签名证书。Windows 首次运行时可能显示“未知发布者”或 Microsoft Defender SmartScreen 信誉提示，这是未签名程序的正常现象。

应从项目正式 Release 获取文件，并使用同一 Release 中的 `SHA256SUMS.txt` 校验：

```powershell
Get-FileHash .\CA-Attendance-System-Setup-3.0.0.exe -Algorithm SHA256
```

结果应与校验文件中对应行完全一致。不要关闭系统安全功能，也不要运行来源不明的同名安装包。

## 8. 源码运行

源码开发需要：

- JDK 21
- Maven 3.9+
- Node.js 22.12+

不需要安装 MySQL。修改前端后执行：

```powershell
cd frontend
npm ci
npm run build

cd ..\backend
mvn package
```

生成的后端文件为：

```text
backend\target\attendance-backend.jar
```

随后双击根目录 `start.bat`，或在 PowerShell 中启动后端。源码版使用电脑上已安装的 JDK；业务数据仍默认写入项目根目录。

## 9. 构建 Windows 发行包

在仓库根目录运行：

```powershell
.\scripts\build-desktop.ps1
```

脚本会：

1. 下载并校验固定版本 Eclipse Temurin JRE 21。
2. 使用干净依赖构建 Vue 前端。
3. 运行 Maven 测试并打包后端。
4. 运行桌面端测试并构建 NSIS 安装程序。
5. 生成便携 ZIP 和 `SHA256SUMS.txt`。

最终文件位于 `release-artifacts\`。构建过程首次需要联网下载依赖，生成的发行包运行时不需要联网。

## 10. 常见问题

源码目录中已有后端 JAR 和 Electron 依赖时，可以在不生成安装包的情况下运行桌面稳定性测试：

```powershell
cd desktop
npm test
cd ..
.\scripts\test-desktop-stability.ps1
```

该脚本使用 `%TEMP%` 下的一次性数据库，验证托盘、单实例、端口绑定、异常退出和整目录迁移，不会操作正式数据库。测试前应先从托盘完全退出正在运行的正式程序。

### 8080 端口已被占用

先确认是否已经启动过本系统。若是其他程序占用，请关闭该程序后重试。桌面版固定使用 8080，以保证浏览器访问地址一致。

### 8081 端口已被占用

8081 是远程管理连接器。请关闭占用该端口的程序后再启动系统；源码版也可以在启动前设置 `APP_REMOTE_PORT` 使用其他端口，并同步修改樱花隧道的本地端口。桌面发行版固定使用 8081。

### 页面无法打开

查看根目录 `logs\desktop.log` 和 `logs\backend.log`。若数据库完整性检查失败，应保留当前 `data` 文件夹并从后台备份恢复，不要直接编辑 SQLite 文件。

### 修改前端后页面没有变化

先运行 `npm run build`，再运行 `mvn package` 并重启服务。浏览器仍缓存旧页面时可使用 `Ctrl + F5`。

### 首次启动没有出现初始化页

说明当前根目录已经存在用户数据。确认程序实际所在根目录，并检查 `data\attendance.db`；不要为了重新初始化而随意删除真实数据库。
