# 变更日志

## 2.6.0 - 2026-08-13

### 数据安全与历史保护

- 统一备份生成与恢复容量契约，改为流式写入、受限解压和临时文件原子发布，避免残缺备份及大数据恢复内存峰值。
- 成员存在签到、培训、排班、维修或审计历史时禁止物理删除，改为引导停用账号，保留业务关系和历史可读性。
- 修复批量审核超过 500 条时静默遗漏的问题，完整处理当前权限范围内全部待审核项，并保证失败整体回滚。

### 业务规则与审计

- 培训时长统一以参与记录的正数时长为唯一统计依据，移除用户界面和接口中的隐藏出勤状态。
- 为统计、培训、维修、操作日志和自定义 Excel 导出补齐统一审计，记录操作人、筛选范围、行数和文件名。
- 统一账号、姓名、密码及成员资料字段校验，Excel 导入提供精确错误行并保持整表原子性。

### 性能与体验

- 维修事务三栏改为独立分页和增量加载，筛选时取消过期请求，完整导出不受页面分页影响。
- 成员编辑、初始化、改密、密码重置和审核页面补齐字段错误、焦点管理及准确结果反馈。

### 升级说明

- 数据库会自动执行 SQLite V10 迁移；历史培训时长保持不变，无需手工运行 SQL。
- 更新前建议从“系统 > 数据”创建完整备份，更新安装版时保留 `C:\CAAttendance\data`、`backups`、`exports` 和 `logs`。
- 安装包仍未使用代码签名证书，请仅从正式 Release 下载并核对 `SHA256SUMS.txt`。

## 2.5.0 - 2026-08-11

### 权限与业务规则

- 建立普通成员、部长、会长和管理员的页面、操作与接口权限验收矩阵。
- 修复成员导入覆盖管理员资料等越权路径，并确保系统始终保留至少一个启用管理员。
- 完善部长本周签到记录处理、统计导出和维修事务权限边界。
- 管理员可分别设置是否强制限制值班日和值班时段；资格规则按签到当时状态保存，不反向改变历史记录。

### 数据安全与一致性

- 将签到审核、手动补录、成员批量状态、培训、排班和维修等高风险写操作纳入事务与审计一致性保护。
- 加固 SQLite 备份恢复，覆盖损坏、重复、未知、过大、高版本和恢复中断场景。
- 修复维修编号并发或删除后重复的问题，改为单调递增序列。
- 删除、停用和高风险维护操作继续保留恢复前安全备份。

### 前端体验

- 精修签到台、签到审核、培训、维修、设置、数据、日志和个人资料页面。
- 完善窄屏布局、长内容、加载与空状态、弹窗焦点和图标按钮可访问名称。
- 修复签到台自动复位、排班同步、审核展示和维修协议信息等问题。
- 后台路由、筛选和导出状态进一步模块化，减少大组件中的业务耦合。

### 性能与桌面端

- 新增可重复的大数据量基线工具，覆盖 500 名成员、10,000 条签到及多年业务数据。
- 全年统计 Excel 导出耗时在测试电脑上由约 3.06 秒降至 1.69 秒。
- 新增桌面真实进程稳定性测试，覆盖托盘、单实例、异常退出、端口释放和整目录迁移。
- 启动前增加 `8081` 端口冲突诊断；源码桌面端优先使用仓库内固定 Temurin 运行时。

### 升级说明

- 数据库会自动执行 SQLite V9 迁移，无需手工运行 SQL。
- 更新安装版时保留 `C:\CAAttendance\data`、`backups`、`exports` 和 `logs`。
- 安装包仍未使用代码签名证书，请从正式 Release 下载并核对 `SHA256SUMS.txt`。

## 2.4.0 - 2026-07-30

- 统一后台视觉规范并优化信息密度。
- 重构固定周排班时间矩阵和成员行级操作。
- 提供 Windows x64 安装版、便携版、内置 Java 运行时和系统托盘支持。
