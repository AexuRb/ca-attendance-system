# 变更日志

## 3.0.0 - 2026-08-24

### 首次安装与桌面显示修复

- 修复空数据库首次启动时初始化状态请求短暂失败后错误跳过初始化页面的问题，首个管理员现在可以正常创建。
- 初始化页与登录页统一字段标签、输入控件、密码显隐、焦点和淡蓝色视觉样式。
- 修复 Electron 视觉缩放范围使用无效零值或负值导致视口宽高为零、窗口只显示淡蓝底色的问题；签到台固定为 100% 视觉缩放，后台使用有效正数范围。
- Windows 打包脚本改为按 UTF-8 读取版本信息，并按扩展名定位便携版启动文件，避免中文系统资源路径解析失败。

### 查询、导出与编号边界

- 成员、考勤、培训、维修和操作日志列表统一默认每页 20 条、最多 100 条；请求页码越界时回落到最后一页。
- 业务 Excel 导出统一最多 50,000 行。
- Excel 导入上限另行执行：排班最多 1,000 行，成员和培训最多 3,000 行。
- 维修编号统一为 `JXWXyyyyMMdd-0001`，末尾为同日四位流水号。

### 测试与发布门禁

- 任务 12 的查询、导出和性能边界已完成收口；CI 从测试报告动态汇总数量，文档不再固定记录测试数量。
- 发布验收区分夹具驱动的 `ui-core-workflows-test.py` 前端渲染/工作流冒烟和真实后端的 `ui-role-regression.py` UI 权限回归。

### 已知风险

- 完整 API 冒烟已同步个人资料的只读年级契约，并在发布前使用当前构建结果复跑。
- 安装包目前没有代码签名，SQLite 主库和备份未加密；发布说明和数据交接必须保留这些风险披露。

### 升级说明

- 启动时会自动执行所需 SQLite 迁移，无需手工运行 SQL；更新前应先创建完整备份。
- 安装版仅替换 `C:\CAAttendance\app`，必须保留同级 `data`、`backups`、`exports` 和 `logs`。

## 2.7.0 - 2026-08-13

### 培训与维修工作区

- 培训场次和参与名单改为服务端分页，支持关键词、日期、页码和当前场次进入 URL；刷新、前进后退和直达链接能够恢复工作位置。
- 培训页面重做为场次列表、场次详情和分页名单工作区；窄屏使用场次切换与详情抽屉，单场 3,000 人名单仍只渲染当前页。
- 维修页面由三列长看板改为状态标签、进行中卡片和历史档案；三类总数由同一筛选查询返回，完整导出不受分页影响。
- 维修历史在宽屏使用表格，在 1,100 像素以下使用紧凑档案卡，修复中窄屏溢出和长设备名称与时间字段粘连。

### 表单与交互可靠性

- 培训和维修的保存、删除、导入、模板下载与导出使用独立操作锁，重复点击不会产生重复请求，也不会阻塞无关区域。
- 编辑弹窗补齐字段标签、输入语义、行内错误和首个错误聚焦；关闭、切换页面或切换记录前会确认未保存修改。
- 培训导入明确显示当前文件并支持移除；维修协议预览加入请求版本保护，迟到响应不能覆盖当前事务。
- 维修详情改为抽屉浏览，联系人电话默认脱敏，查看、协议、编辑和删除操作保持角色权限边界。

### 大数据与桌面验收

- 新增隔离大数据种子、功能验收和四档视口截图工具，覆盖 200 场培训、3,000 人单场名单、1,100 条维修及长内容、空备注和历史快照。
- 新增 Electron 窗口缩放烟测，实际验证 1,080 x 720 与 1,440 x 900，兼容 Windows 窗口边框产生的最多 2 像素尺寸差异。
- 本轮完成前端、后端、脚本和桌面端自动化测试，并完成隔离导入导出、协议、权限与性能回归；具体测试数量以 CI 运行时报告为准。

### 升级说明

- 本版本没有新增数据库迁移，现有 SQLite 数据可直接沿用。
- 更新前仍建议创建完整备份；安装版只替换 `C:\CAAttendance\app`，便携版迁移时保留 `data`、`backups`、`exports` 和 `logs`。
- 安装包未使用代码签名证书，请仅从正式 Release 下载并核对 `SHA256SUMS.txt`。

## 2.6.0 - 2026-08-13

### 数据安全与历史保护

- 统一备份生成与恢复容量契约，改为流式写入、受限解压和临时文件原子发布，避免残缺备份及大数据恢复内存峰值。
- 成员存在签到、培训、排班、维修或审计历史时禁止物理删除，改为引导停用账号，保留业务关系和历史可读性。
- 修复批量审核超过 500 条时静默遗漏的问题，完整处理当前权限范围内全部待审核项，并保证失败整体回滚。

### 业务规则与审计

- 培训时长统一以参与记录的正数时长为唯一统计依据，移除用户界面和接口中的隐藏出勤状态。
- 为统计、培训、维修、操作日志和自定义 Excel 导出补齐统一审计，记录操作人、筛选范围、行数和文件名。
- 统一账号、姓名、密码及成员资料字段校验，Excel 导入提供精确错误行并保持整表原子性。

### 性能与体验

- 维修事务三栏改为独立分页和增量加载，筛选时取消过期请求，完整导出不受页面分页影响。
- 成员编辑、初始化、改密、密码重置和审核页面补齐字段错误、焦点管理及准确结果反馈。

### 升级说明

- 数据库会自动执行 SQLite V10 迁移；历史培训时长保持不变，无需手工运行 SQL。
- 更新前建议从“系统 > 数据”创建完整备份，更新安装版时保留 `C:\CAAttendance\data`、`backups`、`exports` 和 `logs`。
- 安装包仍未使用代码签名证书，请仅从正式 Release 下载并核对 `SHA256SUMS.txt`。

## 2.5.0 - 2026-08-11

### 权限与业务规则

- 建立普通成员、部长、会长和管理员的页面、操作与接口权限验收矩阵。
- 修复成员导入覆盖管理员资料等越权路径，并确保系统始终保留至少一个启用管理员。
- 完善部长本周签到记录处理、统计导出和维修事务权限边界。
- 管理员可分别设置是否强制限制值班日和值班时段；资格规则按签到当时状态保存，不反向改变历史记录。

### 数据安全与一致性

- 将签到审核、手动补录、成员批量状态、培训、排班和维修等高风险写操作纳入事务与审计一致性保护。
- 加固 SQLite 备份恢复，覆盖损坏、重复、未知、过大、高版本和恢复中断场景。
- 修复维修编号并发或删除后重复的问题，改为单调递增序列。
- 删除、停用和高风险维护操作继续保留恢复前安全备份。

### 前端体验

- 精修签到台、签到审核、培训、维修、设置、数据、日志和个人资料页面。
- 完善窄屏布局、长内容、加载与空状态、弹窗焦点和图标按钮可访问名称。
- 修复签到台自动复位、排班同步、审核展示和维修协议信息等问题。
- 后台路由、筛选和导出状态进一步模块化，减少大组件中的业务耦合。

### 性能与桌面端

- 新增可重复的大数据量基线工具，覆盖 500 名成员、10,000 条签到及多年业务数据。
- 全年统计 Excel 导出耗时在测试电脑上由约 3.06 秒降至 1.69 秒。
- 新增桌面真实进程稳定性测试，覆盖托盘、单实例、异常退出、端口释放和整目录迁移。
- 启动前增加 `8081` 端口冲突诊断；源码桌面端优先使用仓库内固定 Temurin 运行时。

### 升级说明

- 数据库会自动执行 SQLite V9 迁移，无需手工运行 SQL。
- 更新安装版时保留 `C:\CAAttendance\data`、`backups`、`exports` 和 `logs`。
- 安装包仍未使用代码签名证书，请从正式 Release 下载并核对 `SHA256SUMS.txt`。

## 2.4.0 - 2026-07-30

- 统一后台视觉规范并优化信息密度。
- 重构固定周排班时间矩阵和成员行级操作。
- 提供 Windows x64 安装版、便携版、内置 Java 运行时和系统托盘支持。
