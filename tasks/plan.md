# 固定周表与前端重构实施计划

1. 建立 Git 检查点和独立分支。
2. 固化固定周表、导入和公开日历的数据与 API 契约。
3. 引入跨模块端口并逐步拆分后端应用层。
4. 升级 SQLite 结构与备份恢复。
5. 实现固定周表和按星期生成的公开排班日历。
6. 使用 TypeScript 重建路由、布局、设计系统和全部页面。
7. 执行后端、前端、桌面端与浏览器验收，更新使用文档。

每个阶段单独运行相关测试。不得主动打包、推送或发布。
