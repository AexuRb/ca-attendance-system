# 学期化重构实施计划

1. 建立 Git 检查点和独立分支。
2. 固化学期、封存、排班例外、调班的数据与 API 契约。
3. 引入跨模块端口和数据库仓储，逐步拆出后端应用层。
4. 新增 V6 迁移，回填历史学期并升级备份恢复。
5. 实现学期生命周期、预检、结算快照、封存和重开。
6. 实现固定排班学期化、排班例外、直接调班和最终排班解析器。
7. 删除旧前端源码与样式，以 TypeScript 重建路由、布局、设计系统和全部页面。
8. 执行后端、前端、桌面端与浏览器验收，更新使用文档。

每个阶段单独运行相关测试并提交本地检查点。不得主动打包、推送或发布。
