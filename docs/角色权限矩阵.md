# 角色权限矩阵

## 1. 使用原则

- 本文档描述当前系统的权限基线，适用于 Windows 桌面端、本机 Web 端和受限远程管理端。
- 前端路由、导航和按钮权限用于减少误操作；后端服务权限才是最终安全边界。
- 普通成员、部长、会长和管理员的账号均可在主机本地登录。
- 远程管理入口只允许会长和管理员登录，且不提供签到台、首次初始化或桌面控制能力。
- 账号停用后，已有登录令牌也必须失效。

## 2. 后台页面权限

| 页面 | 路由 | 成员 | 部长 | 会长 | 管理员 |
| --- | --- | --- | --- | --- | --- |
| 今日概览 | `/admin/today` | 否 | 是 | 是 | 是 |
| 签到审核 | `/admin/reviews` | 否 | 是 | 是 | 是 |
| 值班记录 | `/admin/attendance` | 否 | 是 | 是 | 是 |
| 数据统计 | `/admin/stats` | 否 | 是 | 是 | 是 |
| 排班管理 | `/admin/schedules` | 否 | 否 | 是 | 是 |
| 成员名册 | `/admin/members` | 否 | 否 | 是 | 是 |
| 个人资料 | `/admin/profile` | 是 | 是 | 是 | 是 |
| 维修事务 | `/admin/repairs` | 否 | 是 | 是 | 是 |
| 培训记录 | `/admin/trainings` | 否 | 否 | 是 | 是 |
| 数据与备份 | `/admin/data` | 否 | 否 | 是 | 是 |
| 系统设置 | `/admin/settings` | 否 | 否 | 是 | 是 |
| 操作日志 | `/admin/logs` | 否 | 否 | 否 | 是 |

成员登录后台后只能进入个人资料页面。无权访问的路由必须由前端守卫重定向，同时对应 API 必须由后端拒绝。

## 3. 业务操作权限

| 操作 | 成员 | 部长 | 会长 | 管理员 | 额外边界 |
| --- | --- | --- | --- | --- | --- |
| 本机公开签到、签退 | 是 | 是 | 是 | 是 | 远程入口不可使用 |
| 查看自己的值班和培训记录 | 是 | 是 | 是 | 是 | 只返回当前账号数据 |
| 查看全部值班记录 | 否 | 是 | 是 | 是 | 部长无成员资料页权限 |
| 审核签到、签退 | 否 | 是 | 是 | 是 | 部长不能审核自己的记录 |
| 手动新增值班记录 | 否 | 否 | 是 | 是 | 必须记录原因和操作日志 |
| 修改、删除值班记录 | 否 | 本周成员和部长记录 | 不限 | 不限 | 部长不能处理会长或管理员记录 |
| 查看和导出标准统计 | 否 | 是 | 是 | 是 | 部长只能使用标准统计导出 |
| 查看和维护成员 | 否 | 否 | 是 | 是 | 会长不能修改管理员账号 |
| 任命成员、部长或会长 | 否 | 否 | 是 | 是 | 目标不能是管理员 |
| 任命或维护管理员 | 否 | 否 | 否 | 是 | 系统必须保留至少一个启用管理员 |
| 删除无值班记录成员 | 否 | 否 | 否 | 是 | 删除前自动备份 |
| 管理固定周排班 | 否 | 否 | 是 | 是 | 只能使用已保存的星期和时段 |
| 修改值班星期和时段 | 否 | 否 | 是 | 是 | 部长无修改权限 |
| 修改有效时长限制 | 否 | 否 | 否 | 是 | 会长可查看，规则只影响之后的新签到 |
| 管理和导出培训 | 否 | 否 | 是 | 是 | 个人仍可查看自己的培训记录 |
| 新增、查看和修改维修事务 | 否 | 是 | 是 | 是 | 处理人候选只返回必要账号信息 |
| 预览和打印维修协议 | 否 | 是 | 是 | 是 | 使用维修事务查看权限，不计作 Excel 导出 |
| 导出维修事务 | 否 | 否 | 是 | 是 | 部长不能导出 |
| 将维修事务移入回收站 | 否 | 否 | 是 | 是 | 属于软删除 |
| 查看、恢复、永久删除维修回收站 | 否 | 否 | 否 | 是 | 永久删除前自动备份并核对编号 |
| 自定义导出业务数据 | 否 | 否 | 是 | 是 | 操作日志数据源仅管理员可选 |
| 创建、查看和下载备份 | 否 | 否 | 是 | 是 | 备份包含完整核心数据 |
| 删除或恢复备份 | 否 | 否 | 否 | 是 | 恢复前自动备份当前数据 |
| 查看、导出和清空操作日志 | 否 | 否 | 否 | 是 | 清空前自动备份 |
| 使用受限远程后台 | 否 | 否 | 是 | 是 | 仍受原角色权限约束 |

## 4. 后端权限策略

后端统一通过 `RolePermissionPolicy` 判断功能权限。当前授权如下：

| 后端权限 | 部长 | 会长 | 管理员 |
| --- | --- | --- | --- |
| `ATTENDANCE_MANAGE` | 是 | 是 | 是 |
| `ATTENDANCE_CREATE` | 否 | 是 | 是 |
| `MEMBERS_MANAGE` | 否 | 是 | 是 |
| `MEMBERS_DELETE` | 否 | 否 | 是 |
| `SCHEDULE_MANAGE` | 否 | 是 | 是 |
| `DUTY_SETTINGS_MANAGE` | 否 | 是 | 是 |
| `TRAINING_MANAGE` | 否 | 是 | 是 |
| `REPAIR_MANAGE` | 是 | 是 | 是 |
| `REPAIR_EXPORT` | 否 | 是 | 是 |
| `REPAIR_DELETE` | 否 | 是 | 是 |
| `REPAIR_RECYCLE_BIN` | 否 | 否 | 是 |
| `STATS_VIEW` | 是 | 是 | 是 |
| `STATS_EXPORT` | 是 | 是 | 是 |
| `CUSTOM_EXPORT` | 否 | 是 | 是 |
| `BACKUPS` | 否 | 是 | 是 |
| `BACKUP_ADMIN` | 否 | 否 | 是 |
| `DATA_CENTER` | 否 | 是 | 是 |
| `OPERATION_LOGS` | 否 | 否 | 是 |
| `REMOTE_ADMIN` | 否 | 是 | 是 |

普通成员不拥有上述任何管理权限。个人资料、个人值班记录和个人培训记录由登录账号身份隔离，不通过管理权限开放。

## 5. 特殊边界

### 5.1 会长与管理员账号

- 会长不能新增管理员、把账号调整为管理员、修改管理员资料、停用管理员或重置管理员密码。
- 成员 Excel 导入命中管理员学号时，会长操作必须跳过该行并返回明确问题。
- 管理员调整或停用其他管理员前，系统必须确认仍有至少一个启用的管理员账号。

### 5.2 部长的值班权限

- 部长签到和签退自动通过。
- 部长可处理本周普通成员和部长的值班记录。
- 部长不能处理会长、管理员或自己的待审核记录。
- 部长不能手动新增记录，也不能进入成员、排班、培训、数据中心或系统设置页面；签到判定读取当前星期和时段不授予修改权限。

### 5.3 远程入口

- 远程登录先检查账号角色，只有会长和管理员可以取得登录令牌。
- 远程请求每次都根据数据库中的当前角色和状态重新校验，不能依赖登录时的旧角色。
- 远程端口拒绝公开签到接口、初始化操作和桌面控制接口。
- 远程读取公开排班数据前仍必须完成会长或管理员登录。

## 6. 自动化验证位置

- 后端完整授权集合：`backend/src/test/java/com/ca/attendance/access/RolePermissionPolicyTest.java`
- 后端真实 API 允许与拒绝场景：`backend/src/test/java/com/ca/attendance/access/RoleAuthorizationWebIntegrationTest.java`
- 远程登录和接口限制：`backend/src/test/java/com/ca/attendance/auth/AuthSecurityTest.java`
- 会长导入不得修改管理员：`backend/src/test/java/com/ca/attendance/user/UserTransactionIntegrationTest.java`
- 前端导航与路由一致性：`frontend/src/app/adminNavigation.test.ts`
- 部长值班记录操作边界：`frontend/src/features/attendance/attendanceRecords.test.ts`
- 成员编辑和批量选择边界：`frontend/src/features/members/memberDirectory.test.ts`
- 维修操作按钮：`frontend/src/features/repairs/repairPermissions.test.ts`
- 备份和回收站按钮：`frontend/src/features/maintenance/dataPermissions.test.ts`

调整角色权限时，应同步更新本文档、前端权限测试和后端接口测试。只修改页面按钮或只修改后端枚举都不算完成。
